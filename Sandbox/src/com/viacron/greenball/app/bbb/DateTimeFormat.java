/*
 * WARNING: This file was generated by BitByBitGenerator.xtend (see
 * generateEnumFile()). 
 * Do not edit.
 */
package com.viacron.greenball.app.bbb;

import java.util.EnumSet;
import java.util.HashMap;
import java.util.Map;

public enum DateTimeFormat {
    DATE(0, "Date"),
    TIME(1, "Time"),
    DATETIME(2, "DateTime");

   private static final Map<Integer, DateTimeFormat> lookup = new HashMap<Integer, DateTimeFormat>();

   static {
      for (DateTimeFormat enumType : EnumSet.allOf(DateTimeFormat.class)) {
         lookup.put(enumType.getId(), enumType);
      }
   }

   private final int id;
   private final String value;

   DateTimeFormat(int id, String v) {
      this.id = id;
      this.value = v;
   }

   public int getId() {
      return this.id;
   }

   public String value() {
      return this.value;
   }

   public static DateTimeFormat fromValue(String v) {
      for (DateTimeFormat dateTimeFormat : DateTimeFormat.values()) {
         if (dateTimeFormat.value.equals(v)) {
               return dateTimeFormat;
         }
      }
      throw new IllegalArgumentException(v);
   }

   public static DateTimeFormat toEnum(int id) {
      return lookup.get(id);
   }
}
