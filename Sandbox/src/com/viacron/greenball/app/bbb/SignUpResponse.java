/*
 * WARNING: This file was generated by BitByBitGenerator.xtend (see
 * generateClassFile()). 
 * Do not edit.
 */
package com.viacron.greenball.app.bbb;

import java.nio.charset.Charset;
import java.util.ArrayList;
import java.util.EnumSet;
import java.util.HashMap;
import java.util.List;
import java.util.Map;

import org.apache.log4j.Logger;
import org.antlr.runtime.ANTLRStringStream;
import org.antlr.runtime.CommonTokenStream;
import org.antlr.runtime.RecognitionException;

import com.viacron.utils.StringMessageBuffer.parser.AbstractAttribute;
import com.viacron.utils.StringMessageBuffer.parser.Element;
import com.viacron.utils.StringMessageBuffer.parser.KeyValuePair;
import com.viacron.utils.StringMessageBuffer.parser.SMBGLexer;
import com.viacron.utils.StringMessageBuffer.parser.SMBGParser;

/**
 * The SignUpResponse class defines a message of the messaging system defined
 * by the GreenballMessages grammar. It provides methods for packing a SignUpResponse object
 * into a byte array or string, and for unpacking a byte array or string
 * back into a SignUpResponse object.
 */
public class SignUpResponse {
   private static final int ID = GreenballMessagesEnum.SIGN_UP_RESPONSE.getId();

   /**
    * The number of bits required to represent the SignUpResponseEnum IDs.
    * # of attributes: 4
    * # of PEnumRefs: 0
    */
   private static final int ID_BITS = 2;

   private static GreenballMessagesDirectoryEntry[] theDirectory;
   private static final int DIRECTORY_OFFSET = 3;

   private static Logger log4j = Logger.getLogger(SignUpResponse.class);

   // Entered buildClassEnum()
   private enum SignUpResponseEnum {
      // This is a directory item. It's offset and length cannot be determined
      // until runtime.
      EMAIL_ADDRESS(0, -1, 0, true),

      // This is a directory item. It's offset and length cannot be determined
      // until runtime.
      PASSWORD(1, -1, 0, true),

      // sizeInBits: 8
      ALREADY_IN_USE(2, 16, 0, false),

      // This is a directory item. It's offset and length cannot be determined
      // until runtime.
      SESSION_ID(3, -1, 0, true);

      private final int id;
      private final int offsetInBits;
      private int length;
      private boolean isDirectoryEntry;

      SignUpResponseEnum(int id, int offsetInBits, int length, boolean isDirectoryEntry) {
         this.id = id;
         this.offsetInBits = offsetInBits;
         this.length = length;
         this.isDirectoryEntry = isDirectoryEntry;
      }

      public int getId() {
         return this.id;
      }

      public int getOffsetInBits() {
         return this.offsetInBits;
      }

      public int getLength() {
         return this.length;
      }

      public void setLength(int length) {
         this.length = length;
      }
   } // Leaving buildClassEnum()

   /*
    * The instance variables.
    * See buildInstanceVariables() in JBitByBitGenerator.java.
    */
   private String emailAddress;
   private String password;
   private boolean alreadyInUse;
   private String sessionId;

   private String theErrorMsg;

   /**
    * The SignUpResponse no-arg constructor.
    */
   public SignUpResponse() {}

   /**
    * The SignUpResponse constructor.
    * See buildParameterAnnotations() in JBitByBitGenerator.java.
    *
    * @param emailAddress The emailAddress message attribute.
    * @param password The password message attribute.
    * @param alreadyInUse The alreadyInUse message attribute.
    * @param sessionId The sessionId message attribute.
    */
   public SignUpResponse(String emailAddress,
                         String password,
                         Boolean alreadyInUse,
                         String sessionId) {
      // Entered buildParameterAssignments()
      this.setEmailAddress(emailAddress);
      this.setPassword(password);
      this.setAlreadyInUse(alreadyInUse);
      this.setSessionId(sessionId);
      // Leaving buildParameterAssignments()
   }


   // Entered buildSetErrorMsgMethod()
   private void setErrorMsg(String errorMsg) {
      theErrorMsg = errorMsg;
   }
   // Leaving buildSetErrorMsgMethod()

   // Entered buildGetErrorMsgMethod()
   /**
    * This method returns the error message or null if no error
    * occurred.
    */
   public String getErrorMsg() {
      return theErrorMsg;
   }
   // Leaving buildGetErrorMsgMethod()

   // Entered buildPackMethod()
   /**
    * This method packs a SignUpResponse object into a byte array. If a required attribute is not
    * provided an exception will be thrown.
    *
    * @return A byte array which represents a SignUpResponse message object.
    * @throws MissingAttributeException
    */
   public byte[] pack() throws MissingAttributeException {
      final String METHOD = "pack()";
      log4j.debug("Entered " + METHOD);

      int byteArraySize = getSignUpResponseSize();
      log4j.debug("byteArraySize: " + byteArraySize);
      byte[] bb = new byte[byteArraySize];

      /*
       * Convert object to byte array.
       */
      int offset = 0;

      bb = GreenballMessagesUtility.insertShort(bb, offset, (short) ID);
      offset += 2;

      if (this.getAlreadyInUse() != null) {
         log4j.debug(String.format("inserting %s at offset %d", "alreadyInUse", offset));
         bb = GreenballMessagesUtility.insertBitfield(bb, offset * 8, 1, this.getAlreadyInUse() == true ? 1 : 0);
         offset += 1;
      } else {
         /*
          * Missing required attribute, generate an error message and throw
          * an exception.
          */
         setErrorMsg("Error packing object. The attribute \"alreadyInUse\" is required, but no value was provided.");
         log4j.error(getErrorMsg());
         throw new MissingAttributeException(getErrorMsg());
      }


      SignUpResponseEnum.EMAIL_ADDRESS.setLength(this.getEmailAddress() != null ? this.getEmailAddress().length() : 0);
      SignUpResponseEnum.PASSWORD.setLength(this.getPassword() != null ? this.getPassword().length() : 0);
      SignUpResponseEnum.SESSION_ID.setLength(this.getSessionId() != null ? this.getSessionId().length() : 0);

      int directorySize = getSignUpResponseDirectorySize();
      if (directorySize != 0) {
         // Create the directory
         theDirectory = new GreenballMessagesDirectoryEntry[directorySize];
         for (int i = 0; i < directorySize; i++) {
            theDirectory[i] = new GreenballMessagesDirectoryEntry();
         }

         // Fill the directory
         int index = 0;
         offset = DIRECTORY_OFFSET
               + GreenballMessagesDirectoryEntry.DIRECTORY_ENTRY_SIZE_IN_BYTES
               * theDirectory.length;

         for (SignUpResponseEnum signUpResponseEnum : SignUpResponseEnum.values()) {
            if (signUpResponseEnum.isDirectoryEntry == true) {
               int id = signUpResponseEnum.getId();
               int length = signUpResponseEnum.getLength();

               theDirectory[index].setId(id);
               theDirectory[index].setLength(length);
               theDirectory[index].setOffset(offset);

               // Insert the data
               if (id == SignUpResponseEnum.EMAIL_ADDRESS.getId()) {
                  log4j.debug(String.format("inserting %s at offset %d", "emailAddress", offset));
                  if (getEmailAddress() != null) {
                     bb = GreenballMessagesUtility.insertString(bb, offset, getEmailAddress());
                     offset += length * 1;
                  }
               }
               else if (id == SignUpResponseEnum.PASSWORD.getId()) {
                  log4j.debug(String.format("inserting %s at offset %d", "password", offset));
                  if (getPassword() != null) {
                     bb = GreenballMessagesUtility.insertString(bb, offset, getPassword());
                     offset += length * 1;
                  }
               }
               else if (id == SignUpResponseEnum.SESSION_ID.getId()) {
                  log4j.debug(String.format("inserting %s at offset %d", "sessionId", offset));
                  if (getSessionId() != null) {
                     bb = GreenballMessagesUtility.insertString(bb, offset, getSessionId());
                     offset += length * 1;
                  }
               }
               index++;
            }
         }

         // Insert the directory
         log4j.debug(String.format("inserting the directory at offset %d", DIRECTORY_OFFSET));
         offset = DIRECTORY_OFFSET;
         index = 0;
         for (GreenballMessagesDirectoryEntry directoryEntry : theDirectory) {
            log4j.debug(String.format("index[%d]: ", index));
            log4j.debug(String.format("  ID:     %d", directoryEntry.getId()));
            log4j.debug(String.format("  length: %d", directoryEntry.getLength()));
            log4j.debug(String.format("  offset: %d", directoryEntry.getOffset()));
            bb = GreenballMessagesUtility.insertInteger(bb, offset, directoryEntry.getId());
            bb = GreenballMessagesUtility.insertInteger(bb, offset + 4, directoryEntry.getLength());
            bb = GreenballMessagesUtility.insertInteger(bb, offset + 8, directoryEntry.getOffset());

            offset += GreenballMessagesDirectoryEntry.DIRECTORY_ENTRY_SIZE_IN_BYTES;
            index++;
         }

         // Calculate and insert the checksum
         long checksum = GreenballMessagesUtility.getChecksum(bb, bb.length - 8);
         bb = GreenballMessagesUtility.insertLong(bb, bb.length - 8, checksum);
      }
      log4j.debug("Leaving " + METHOD);
      return bb;
   }
   // Leaving buildPackMethod()

   // Entered buildUnpackMethod()
   /**
    * This method unpacks a byte array containing a SignUpResponse object.
    *
    * @return The byte array as a SignUpResponse message object.
    */
   public SignUpResponse unpack(byte[] ba) {
      final String METHOD = "unpack()";
      log4j.debug("Entered " + METHOD);

      SignUpResponse signUpResponse = new SignUpResponse();

      signUpResponse.setAlreadyInUse(GreenballMessagesUtility.getBoolean(ba, SignUpResponseEnum.ALREADY_IN_USE.getOffsetInBits()));

      // Get the directory
      int directorySize = getSignUpResponseDirectorySize();
      theDirectory = new GreenballMessagesDirectoryEntry[directorySize];
      int dirOffset = DIRECTORY_OFFSET;

      for (int entry = 0 ; entry < directorySize; entry++) {
         GreenballMessagesDirectoryEntry directoryEntry = new GreenballMessagesDirectoryEntry();

         int id = GreenballMessagesUtility.getInteger(ba, dirOffset * 8);
         directoryEntry.setId(id);
         int length = GreenballMessagesUtility.getInteger(ba, (dirOffset + 4) * 8);
         directoryEntry.setLength(length);
         int offset = GreenballMessagesUtility.getInteger(ba, (dirOffset + 8) * 8);
         directoryEntry.setOffset(offset);

         theDirectory[entry] = directoryEntry;

         dirOffset += GreenballMessagesDirectoryEntry.DIRECTORY_ENTRY_SIZE_IN_BYTES;
      }

      for (GreenballMessagesDirectoryEntry directoryEntry : theDirectory) {
         int id = directoryEntry.getId();
         int offset = directoryEntry.getOffset();
         int length = directoryEntry.getLength();

         if (id == SignUpResponseEnum.EMAIL_ADDRESS.getId()) {
            signUpResponse.setEmailAddress(GreenballMessagesUtility.getString(ba, offset, length));
         }
         else if (id == SignUpResponseEnum.PASSWORD.getId()) {
            signUpResponse.setPassword(GreenballMessagesUtility.getString(ba, offset, length));
         }
         else if (id == SignUpResponseEnum.SESSION_ID.getId()) {
            signUpResponse.setSessionId(GreenballMessagesUtility.getString(ba, offset, length));
         }
      }

      log4j.debug("Leaving " + METHOD);
      return signUpResponse;
   }
   // Leaving buildUnpackMethod()

   // Entered buildPackStrMethod()
   /**
    * This method packs a SignUpResponse object into a String. If a required attribute is not
    * provided an exception will be thrown.
    *
    * @return A String which represents a SignUpResponse message object.
    * @throws MissingAttributeException
    */
   public String packStr() throws MissingAttributeException {
      final String METHOD = "packStr()";
      log4j.debug("Entered " + METHOD);

      String msg = null;
      setErrorMsg(null);
       
      /*
       * Initialize attribute values.
       */
      InitializeSignUpResponse initializeSignUpResponse = new InitializeSignUpResponse(this);
      Element element = initializeSignUpResponse.initialize();

      if (getErrorMsg() == null) {
         /*
          * Convert object to String.
          * See convertObjectToString() in JBitByBitGenerator.java.
          */
         msg = "{" + element.getName();
         for (AbstractAttribute attr : element.getAbstractAttributes()) {
            msg = buildString(attr, msg);
         }
         msg = msg.concat("}");

         log4j.debug("msg: " + msg);
      }

      log4j.debug("Leaving " + METHOD);
      return msg;
   }
   // Leaving buildPackStrMethod()

   // Entered buildUnpackStrMethod()
   /**
    * The unpack() method unpacks a String into an object. The String must conform to the
    * SMBG grammar.
    *
     * @return The message string as an object.
    */
   public SignUpResponse unpack(String buffer) {
      final String METHOD = "unpack()";
      log4j.debug("Entered " + METHOD);

      setErrorMsg(null);

      // Create the lexer
      log4j.debug("lexing...");
      SMBGLexer lexer = new SMBGLexer(new ANTLRStringStream(buffer));

      int nbrOfSyntaxErrors = lexer.getNumberOfSyntaxErrors();
      log4j.debug("# of lexer errors: " + nbrOfSyntaxErrors);

      // Check for errors
      if (nbrOfSyntaxErrors != 0) {
         for (String error : lexer.getErrors()) {
            log4j.error(error);
         }

         return null;
      }

      // Wrap a token-stream around the lexer
      CommonTokenStream tokenStream = new CommonTokenStream(lexer);

      // Create the parser
      log4j.debug("parsing...");
      SMBGParser parser = new SMBGParser(tokenStream);

      // Check for errors
      nbrOfSyntaxErrors = parser.getNumberOfSyntaxErrors();
      log4j.debug("# of parse errors: " + nbrOfSyntaxErrors);

      if (nbrOfSyntaxErrors != 0) {
         for (String error : parser.getErrors()) {
               log4j.error(error);
         }

         return null;
      }

      // Unpack the string into objects
      log4j.debug("unpacking...");
      try {
         parser.unpack();
      } catch (RecognitionException e) {
         log4j.error("Error: " + e.getMessage());
         setErrorMsg(e.getMessage());
         return null;
      }
      
      Element element = parser.getElement();
      element.dump();

      /*
       * Instantiate the SignUpResponse object.
       */
       InstantiateSignUpResponse instantiateSignUpResponse = new InstantiateSignUpResponse(element);
       SignUpResponse signUpResponse = instantiateSignUpResponse.instantiate();

      /*
       * Verify that a value was provided for all mandatory attributes.
       */
      String result = instantiateSignUpResponse.validate();
      if (result != null) {
         log4j.error("Error: " + result);
         setErrorMsg(result);
         return null;
      }

      log4j.debug("Leaving " + METHOD);
      return signUpResponse;
   }
   // Leaving buildUnpackStrMethod()

   private String buildString(AbstractAttribute attr, String msg) {
      for (KeyValuePair kvp : attr.getKeyValuePairs()) {
         msg = msg.concat(" " + kvp.getKey() + ":" + kvp.getValue());
      }

      for (Element element : attr.getElements()) {
         msg = msg.concat(" {" + element.getName());
         for (AbstractAttribute abstractAttribute : element.getAbstractAttributes()) {
            msg = buildString(abstractAttribute, msg);
         }
         msg = msg.concat("}");
      }

      return msg;
   }

   /**
    * This method sets the emailAddress attribute. Setting of this value is required.
    * @param emailAddress The emailAddress attribute.
    */
   public void setEmailAddress(String emailAddress) {
      this.emailAddress = emailAddress;
   }

   /**
    * This method sets the password attribute. Setting of this value is required.
    * @param password The password attribute.
    */
   public void setPassword(String password) {
      this.password = password;
   }

   /**
    * This method sets the alreadyInUse attribute. Setting of this value is required.
    * @param alreadyInUse The alreadyInUse attribute.
    */
   public void setAlreadyInUse(Boolean alreadyInUse) {
      this.alreadyInUse = alreadyInUse;
   }

   /**
    * This method sets the sessionId attribute. Setting of this value is optional.
    * @param sessionId The sessionId attribute.
    */
   public void setSessionId(String sessionId) {
      this.sessionId = sessionId;
   }

   /**
    * This method gets the emailAddress attribute. The emailAddress attribute is a required attribute.
    */
   public String getEmailAddress() {
      return this.emailAddress;
   }

   /**
    * This method gets the password attribute. The password attribute is a required attribute.
    */
   public String getPassword() {
      return this.password;
   }

   /**
    * This method gets the alreadyInUse attribute. The alreadyInUse attribute is a required attribute.
    */
   public Boolean getAlreadyInUse() {
      return this.alreadyInUse;
   }

   /**
    * This method gets the sessionId attribute. The sessionId attribute is an optional attribute.
    */
   public String getSessionId() {
      return this.sessionId;
   }

   /**
    * This method gets the size of the directory.
    */
   private int getSignUpResponseDirectorySize() {
      int directorySize = 0;

      // emailAddress
      directorySize++;
      // password
      directorySize++;
      // sessionId
      directorySize++;

      return directorySize;
   }


   /**
    * This method gets the size of the SignUpResponse object.
    * @return The size in bytes.
    */
   public int getSignUpResponseSize() {
      int size = 0;

      int directorySize = getSignUpResponseDirectorySize();
      size += directorySize * GreenballMessagesDirectoryEntry.DIRECTORY_ENTRY_SIZE_IN_BYTES;
      // Add two bytes for the ID
      size += 2;
      // Add eight bytes for the CRC
      size += 8;
      // Add size of emailAddress
      size += (getEmailAddress() != null ? getEmailAddress().length() : 0);
      // Add size of password
      size += (getPassword() != null ? getPassword().length() : 0);
      // Add size of alreadyInUse
      size += 1;
      // Add size of sessionId
      size += (getSessionId() != null ? getSessionId().length() : 0);

      return size;
   }

   @Override
   public String toString() {
      StringBuilder result = new StringBuilder();
      String NEW_LINE = System.getProperty("line.separator");

      result.append(this.getClass().getSimpleName() + " Object {" + NEW_LINE);
      result.append(" emailAddress: " + emailAddress + NEW_LINE);
      result.append(" password: " + password + NEW_LINE);
      result.append(" alreadyInUse: " + alreadyInUse + NEW_LINE);
      result.append(" sessionId: " + sessionId + NEW_LINE);

      result.append("} ");

      return result.toString();
   }

   /**
    * The hashCode method returns this object's hashCode.
    */
   @Override
   public int hashCode() {
      final int prime = 31;
      int result = 1;
      result = prime * result
         + ((String) emailAddress).hashCode();
      result = prime * result
         + ((String) password).hashCode();
      result = prime * result
         + ((Boolean) alreadyInUse).hashCode();
      result = prime * result
         + ((String) sessionId).hashCode();
      return result;
   }

   /**
    * The equals method checks for equality. 
    */
   public boolean equals(Object obj) {
      if (obj == this) return true;
      if (obj == null) return false;
      if (getClass() != obj.getClass()) return false;

      SignUpResponse signUpResponse = (SignUpResponse) obj;
      if (!signUpResponse.getEmailAddress().equals(this.getEmailAddress())) {
         return false;
      }

      if (!signUpResponse.getPassword().equals(this.getPassword())) {
         return false;
      }

      if (signUpResponse.getAlreadyInUse() != this.getAlreadyInUse()) {
         return false;
      }

      if (signUpResponse.getSessionId() == null && this.getSessionId() != null) {
         return false;
      }
      else if (signUpResponse.getSessionId() != null && this.getSessionId() == null) {
         return false;
      }
      else if (signUpResponse.getSessionId() != null && 
      !signUpResponse.getSessionId().equals(this.getSessionId())) {
         return false;
      }

      return true;
   }
}
