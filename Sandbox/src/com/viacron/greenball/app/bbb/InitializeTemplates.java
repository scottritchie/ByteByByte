/*
 * WARNING: This file was generated by BitByBitGenerator.xtend (see
 * generateInitializationFile()). 
 * Do not edit.
 */
package com.viacron.greenball.app.bbb;
import java.util.ArrayList;
import java.util.List;

import org.apache.log4j.Logger;

import com.viacron.utils.StringMessageBuffer.parser.AbstractAttribute;
import com.viacron.utils.StringMessageBuffer.parser.Element;
import com.viacron.utils.StringMessageBuffer.parser.KeyValuePair;

/**
 * The InitializeTemplates class initializes the element object with the Templates object.
 *
 */
public class InitializeTemplates {
   private static Logger log4j = Logger.getLogger(InitializeTemplates.class);
   private String theErrorMsg;
   private Element element;
   private Templates templates;

   /**
    * The InitializeTemplates class initializes the element object with the Templates object.
    *
    * @param templates The object which will be used to initialize the element object.
    */
   public InitializeTemplates(Templates templates) {
      this.templates = templates;

      this.element = new Element("Templates");
   }

   /**
    * This method initializes the element.
    */
   public Element initialize() throws MissingAttributeException {
      final String METHOD = "initialize()";
      log4j.debug("Entered " + METHOD);

      log4j.debug("element.getName(): Templates");

      if (templates.getTemplates() != null) {
         AbstractAttribute abstractAttribute = initializeTemplates(templates.getTemplates());
         element.getAbstractAttributes().add(abstractAttribute);
      }


      if (templates.getSessionId() != null) {
         AbstractAttribute abstractAttribute = initializeSessionId(templates.getSessionId());
         element.getAbstractAttributes().add(abstractAttribute);
      }
      else {
         // Missing required attribute, generate an error message and throw an exception
         setErrorMsg("Error packing object. The attribute \"sessionId\" is required, but no value was provided.");
         log4j.error(getErrorMsg());
         throw new MissingAttributeException(getErrorMsg());
      }


      log4j.debug("Leaving " + METHOD);
      return element;
   }

   /*
    * This method initializes the templates attribute.
    * Generated by initAttributeMethods()
    */
   private AbstractAttribute initializeTemplates(List<String> templatesList)
           throws MissingAttributeException {
      AbstractAttribute abstractAttribute = new AbstractAttribute();
      // Entered initAbstractAttributeValue()
      int templatesIndex = 0;
      for (String templates : templatesList) {
         // Entered initAttributeValue()
         if (templates != null) {
            log4j.debug("Creating new KeyValuePair: templates[" + templatesIndex + "]");
            KeyValuePair kvp = new KeyValuePair("templates[" + templatesIndex + "]", "<STRING_BEGIN>" + templates + "<STRING_END>");
            abstractAttribute.getKeyValuePairs().add(kvp);
         }

         // Leaving initAttributeValue()
         templatesIndex++;
      }
      // Leaving initAbstractAttributeValue()
      return abstractAttribute;
   }

   /*
    * This method initializes the sessionId attribute.
    * Generated by initAttributeMethods()
    */
   private AbstractAttribute initializeSessionId(String sessionId)
         throws MissingAttributeException {
      AbstractAttribute abstractAttribute = new AbstractAttribute();
      // Entered initAbstractAttributeValue()
      // Entered initAttributeValue()
      if (sessionId != null) {
         log4j.debug("Creating new KeyValuePair: sessionId");
         KeyValuePair kvp = new KeyValuePair("sessionId", "<STRING_BEGIN>" + sessionId + "<STRING_END>");
         abstractAttribute.getKeyValuePairs().add(kvp);
      }
      else {
         // Missing required attribute, generate an error message and throw an exception
         setErrorMsg("Error packing object. The attribute \"sessionId\" is required, but no value was provided.");
         log4j.error(getErrorMsg());
         throw new MissingAttributeException(getErrorMsg());
      }

      // Leaving initAttributeValue()
      // Leaving initAbstractAttributeValue()
      return abstractAttribute;
   }

   /**
   * This method returns the error message or null if no error occurred.
   */
   public String getErrorMsg() {
      return theErrorMsg;
   }

   private void setErrorMsg(String errorMsg) {
      theErrorMsg = errorMsg;
   }

}
