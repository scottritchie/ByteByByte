/*
 * WARNING: This file was generated by BitByBitGenerator.xtend (see
 * generateEnumFile()). 
 * Do not edit.
 */
package com.viacron.greenball.app.bbb;

import java.util.EnumSet;
import java.util.HashMap;
import java.util.Map;

public enum AppState {
    STARTINGUP(0, "StartingUp"),
    CONNECTINGTOSERVER(1, "ConnectingToServer"),
    CONNECTEDTOSERVER(2, "ConnectedToServer"),
    LOGGINGIN(3, "LoggingIn"),
    LOGGEDIN(4, "LoggedIn"),
    SHUTTINGDOWN(5, "ShuttingDown"),
    SHUTDOWN(6, "Shutdown");

   private static final Map<Integer, AppState> lookup = new HashMap<Integer, AppState>();

   static {
      for (AppState enumType : EnumSet.allOf(AppState.class)) {
         lookup.put(enumType.getId(), enumType);
      }
   }

   private final int id;
   private final String value;

   AppState(int id, String v) {
      this.id = id;
      this.value = v;
   }

   public int getId() {
      return this.id;
   }

   public String value() {
      return this.value;
   }

   public static AppState fromValue(String v) {
      for (AppState appState : AppState.values()) {
         if (appState.value.equals(v)) {
               return appState;
         }
      }
      throw new IllegalArgumentException(v);
   }

   public static AppState toEnum(int id) {
      return lookup.get(id);
   }
}
