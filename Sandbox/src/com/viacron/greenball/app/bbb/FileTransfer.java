/*
 * WARNING: This file was generated by BitByBitGenerator.xtend (see
 * generateClassFile()). 
 * Do not edit.
 */
package com.viacron.greenball.app.bbb;

import java.nio.charset.Charset;
import java.util.ArrayList;
import java.util.EnumSet;
import java.util.HashMap;
import java.util.List;
import java.util.Map;

import org.apache.log4j.Logger;
import org.antlr.runtime.ANTLRStringStream;
import org.antlr.runtime.CommonTokenStream;
import org.antlr.runtime.RecognitionException;

import com.viacron.utils.StringMessageBuffer.parser.AbstractAttribute;
import com.viacron.utils.StringMessageBuffer.parser.Element;
import com.viacron.utils.StringMessageBuffer.parser.KeyValuePair;
import com.viacron.utils.StringMessageBuffer.parser.SMBGLexer;
import com.viacron.utils.StringMessageBuffer.parser.SMBGParser;

/**
 * The FileTransfer class defines a message of the messaging system defined
 * by the GreenballMessages grammar. It provides methods for packing a FileTransfer object
 * into a byte array or string, and for unpacking a byte array or string
 * back into a FileTransfer object.
 */
public class FileTransfer {
   private static final int ID = GreenballMessagesEnum.FILE_TRANSFER.getId();

   /**
    * The number of bits required to represent the FileTransferEnum IDs.
    * # of attributes: 8
    * # of PEnumRefs: 0
    */
   private static final int ID_BITS = 3;

   private static GreenballMessagesDirectoryEntry[] theDirectory;
   private static final int DIRECTORY_OFFSET = 7;

   private static Logger log4j = Logger.getLogger(FileTransfer.class);

   // Entered buildClassEnum()
   private enum FileTransferEnum {
      // This is a directory item. It's offset and length cannot be determined
      // until runtime.
      FILE_NAME(0, -1, 0, true),

      // This is a directory item. It's offset and length cannot be determined
      // until runtime.
      UUID(1, -1, 0, true),

      // This is a directory item. It's offset and length cannot be determined
      // until runtime.
      ORG_ID(2, -1, 0, true),

      // This is a directory item. It's offset and length cannot be determined
      // until runtime.
      ORG_NAME(3, -1, 0, true),

      // This is a directory item. It's offset and length cannot be determined
      // until runtime.
      USER_NAME(4, -1, 0, true),

      // sizeInBits: 32
      CHUNK_NUMBER(5, 16, 0, false),

      // sizeInBits: 8
      LAST_CHUNK(6, 48, 0, false),

      // This is a directory item. It's offset and length cannot be determined
      // until runtime.
      CHUNK(7, -1, 0, true);

      private final int id;
      private final int offsetInBits;
      private int length;
      private boolean isDirectoryEntry;

      FileTransferEnum(int id, int offsetInBits, int length, boolean isDirectoryEntry) {
         this.id = id;
         this.offsetInBits = offsetInBits;
         this.length = length;
         this.isDirectoryEntry = isDirectoryEntry;
      }

      public int getId() {
         return this.id;
      }

      public int getOffsetInBits() {
         return this.offsetInBits;
      }

      public int getLength() {
         return this.length;
      }

      public void setLength(int length) {
         this.length = length;
      }
   } // Leaving buildClassEnum()

   /*
    * The instance variables.
    * See buildInstanceVariables() in JBitByBitGenerator.java.
    */
   private String fileName;
   private String uuid;
   private String orgId;
   private String orgName;
   private String userName;
   private int chunkNumber;
   private boolean lastChunk;
   private List<Byte> chunk;

   private String theErrorMsg;

   /**
    * The FileTransfer no-arg constructor.
    */
   public FileTransfer() {}

   /**
    * The FileTransfer constructor.
    * See buildParameterAnnotations() in JBitByBitGenerator.java.
    *
    * @param fileName The fileName message attribute.
    * @param uuid The uuid message attribute.
    * @param orgId The orgId message attribute.
    * @param orgName The orgName message attribute.
    * @param userName The userName message attribute.
    * @param chunkNumber The chunkNumber message attribute.
    * @param lastChunk The lastChunk message attribute.
    * @param chunk The chunk message attribute.
    */
   public FileTransfer(String fileName,
                       String uuid,
                       String orgId,
                       String orgName,
                       String userName,
                       Integer chunkNumber,
                       Boolean lastChunk,
                       List<Byte> chunk) {
      // Entered buildParameterAssignments()
      this.setFileName(fileName);
      this.setUuid(uuid);
      this.setOrgId(orgId);
      this.setOrgName(orgName);
      this.setUserName(userName);
      this.setChunkNumber(chunkNumber);
      this.setLastChunk(lastChunk);
      this.setChunk(chunk);
      // Leaving buildParameterAssignments()
   }


   // Entered buildSetErrorMsgMethod()
   private void setErrorMsg(String errorMsg) {
      theErrorMsg = errorMsg;
   }
   // Leaving buildSetErrorMsgMethod()

   // Entered buildGetErrorMsgMethod()
   /**
    * This method returns the error message or null if no error
    * occurred.
    */
   public String getErrorMsg() {
      return theErrorMsg;
   }
   // Leaving buildGetErrorMsgMethod()

   // Entered buildPackMethod()
   /**
    * This method packs a FileTransfer object into a byte array. If a required attribute is not
    * provided an exception will be thrown.
    *
    * @return A byte array which represents a FileTransfer message object.
    * @throws MissingAttributeException
    */
   public byte[] pack() throws MissingAttributeException {
      final String METHOD = "pack()";
      log4j.debug("Entered " + METHOD);

      int byteArraySize = getFileTransferSize();
      log4j.debug("byteArraySize: " + byteArraySize);
      byte[] bb = new byte[byteArraySize];

      /*
       * Convert object to byte array.
       */
      int offset = 0;

      bb = GreenballMessagesUtility.insertShort(bb, offset, (short) ID);
      offset += 2;

      if (this.getChunkNumber() != null) {
         log4j.debug(String.format("inserting %s at offset %d", "chunkNumber", offset));
         bb = GreenballMessagesUtility.insertInteger(bb, offset, this.getChunkNumber());
         offset += 4;
      } else {
         /*
          * Missing required attribute, generate an error message and throw
          * an exception.
          */
         setErrorMsg("Error packing object. The attribute \"chunkNumber\" is required, but no value was provided.");
         log4j.error(getErrorMsg());
         throw new MissingAttributeException(getErrorMsg());
      }

      if (this.getLastChunk() != null) {
         log4j.debug(String.format("inserting %s at offset %d", "lastChunk", offset));
         bb = GreenballMessagesUtility.insertBitfield(bb, offset * 8, 1, this.getLastChunk() == true ? 1 : 0);
         offset += 1;
      } else {
         /*
          * Missing required attribute, generate an error message and throw
          * an exception.
          */
         setErrorMsg("Error packing object. The attribute \"lastChunk\" is required, but no value was provided.");
         log4j.error(getErrorMsg());
         throw new MissingAttributeException(getErrorMsg());
      }


      FileTransferEnum.FILE_NAME.setLength(this.getFileName() != null ? this.getFileName().length() : 0);
      FileTransferEnum.UUID.setLength(this.getUuid() != null ? this.getUuid().length() : 0);
      FileTransferEnum.ORG_ID.setLength(this.getOrgId() != null ? this.getOrgId().length() : 0);
      FileTransferEnum.ORG_NAME.setLength(this.getOrgName() != null ? this.getOrgName().length() : 0);
      FileTransferEnum.USER_NAME.setLength(this.getUserName() != null ? this.getUserName().length() : 0);
      FileTransferEnum.CHUNK.setLength(this.getChunk() != null ? this.getChunk().size() : 0);

      int directorySize = getFileTransferDirectorySize();
      if (directorySize != 0) {
         // Create the directory
         theDirectory = new GreenballMessagesDirectoryEntry[directorySize];
         for (int i = 0; i < directorySize; i++) {
            theDirectory[i] = new GreenballMessagesDirectoryEntry();
         }

         // Fill the directory
         int index = 0;
         offset = DIRECTORY_OFFSET
               + GreenballMessagesDirectoryEntry.DIRECTORY_ENTRY_SIZE_IN_BYTES
               * theDirectory.length;

         for (FileTransferEnum fileTransferEnum : FileTransferEnum.values()) {
            if (fileTransferEnum.isDirectoryEntry == true) {
               int id = fileTransferEnum.getId();
               int length = fileTransferEnum.getLength();

               theDirectory[index].setId(id);
               theDirectory[index].setLength(length);
               theDirectory[index].setOffset(offset);

               // Insert the data
               if (id == FileTransferEnum.FILE_NAME.getId()) {
                  log4j.debug(String.format("inserting %s at offset %d", "fileName", offset));
                  if (getFileName() != null) {
                     bb = GreenballMessagesUtility.insertString(bb, offset, getFileName());
                     offset += length * 1;
                  }
               }
               else if (id == FileTransferEnum.UUID.getId()) {
                  log4j.debug(String.format("inserting %s at offset %d", "uuid", offset));
                  if (getUuid() != null) {
                     bb = GreenballMessagesUtility.insertString(bb, offset, getUuid());
                     offset += length * 1;
                  }
               }
               else if (id == FileTransferEnum.ORG_ID.getId()) {
                  log4j.debug(String.format("inserting %s at offset %d", "orgId", offset));
                  if (getOrgId() != null) {
                     bb = GreenballMessagesUtility.insertString(bb, offset, getOrgId());
                     offset += length * 1;
                  }
               }
               else if (id == FileTransferEnum.ORG_NAME.getId()) {
                  log4j.debug(String.format("inserting %s at offset %d", "orgName", offset));
                  if (getOrgName() != null) {
                     bb = GreenballMessagesUtility.insertString(bb, offset, getOrgName());
                     offset += length * 1;
                  }
               }
               else if (id == FileTransferEnum.USER_NAME.getId()) {
                  log4j.debug(String.format("inserting %s at offset %d", "userName", offset));
                  if (getUserName() != null) {
                     bb = GreenballMessagesUtility.insertString(bb, offset, getUserName());
                     offset += length * 1;
                  }
               }
               else if (id == FileTransferEnum.CHUNK.getId()) {
                  log4j.debug(String.format("inserting %s at offset %d", "chunk", offset));
                  log4j.debug(String.format("inserting %s at offset %d", "chunk", offset));
                  bb = GreenballMessagesUtility.insertBytes(bb, offset, getChunk());
                  offset += length * 1;
               }
               index++;
            }
         }

         // Insert the directory
         log4j.debug(String.format("inserting the directory at offset %d", DIRECTORY_OFFSET));
         offset = DIRECTORY_OFFSET;
         index = 0;
         for (GreenballMessagesDirectoryEntry directoryEntry : theDirectory) {
            log4j.debug(String.format("index[%d]: ", index));
            log4j.debug(String.format("  ID:     %d", directoryEntry.getId()));
            log4j.debug(String.format("  length: %d", directoryEntry.getLength()));
            log4j.debug(String.format("  offset: %d", directoryEntry.getOffset()));
            bb = GreenballMessagesUtility.insertInteger(bb, offset, directoryEntry.getId());
            bb = GreenballMessagesUtility.insertInteger(bb, offset + 4, directoryEntry.getLength());
            bb = GreenballMessagesUtility.insertInteger(bb, offset + 8, directoryEntry.getOffset());

            offset += GreenballMessagesDirectoryEntry.DIRECTORY_ENTRY_SIZE_IN_BYTES;
            index++;
         }

         // Calculate and insert the checksum
         long checksum = GreenballMessagesUtility.getChecksum(bb, bb.length - 8);
         bb = GreenballMessagesUtility.insertLong(bb, bb.length - 8, checksum);
      }
      log4j.debug("Leaving " + METHOD);
      return bb;
   }
   // Leaving buildPackMethod()

   // Entered buildUnpackMethod()
   /**
    * This method unpacks a byte array containing a FileTransfer object.
    *
    * @return The byte array as a FileTransfer message object.
    */
   public FileTransfer unpack(byte[] ba) {
      final String METHOD = "unpack()";
      log4j.debug("Entered " + METHOD);

      FileTransfer fileTransfer = new FileTransfer();

      fileTransfer.setChunkNumber(GreenballMessagesUtility.getInteger(ba, FileTransferEnum.CHUNK_NUMBER.getOffsetInBits()));
      fileTransfer.setLastChunk(GreenballMessagesUtility.getBoolean(ba, FileTransferEnum.LAST_CHUNK.getOffsetInBits()));

      // Get the directory
      int directorySize = getFileTransferDirectorySize();
      theDirectory = new GreenballMessagesDirectoryEntry[directorySize];
      int dirOffset = DIRECTORY_OFFSET;

      for (int entry = 0 ; entry < directorySize; entry++) {
         GreenballMessagesDirectoryEntry directoryEntry = new GreenballMessagesDirectoryEntry();

         int id = GreenballMessagesUtility.getInteger(ba, dirOffset * 8);
         directoryEntry.setId(id);
         int length = GreenballMessagesUtility.getInteger(ba, (dirOffset + 4) * 8);
         directoryEntry.setLength(length);
         int offset = GreenballMessagesUtility.getInteger(ba, (dirOffset + 8) * 8);
         directoryEntry.setOffset(offset);

         theDirectory[entry] = directoryEntry;

         dirOffset += GreenballMessagesDirectoryEntry.DIRECTORY_ENTRY_SIZE_IN_BYTES;
      }

      for (GreenballMessagesDirectoryEntry directoryEntry : theDirectory) {
         int id = directoryEntry.getId();
         int offset = directoryEntry.getOffset();
         int length = directoryEntry.getLength();

         if (id == FileTransferEnum.FILE_NAME.getId()) {
            fileTransfer.setFileName(GreenballMessagesUtility.getString(ba, offset, length));
         }
         else if (id == FileTransferEnum.UUID.getId()) {
            fileTransfer.setUuid(GreenballMessagesUtility.getString(ba, offset, length));
         }
         else if (id == FileTransferEnum.ORG_ID.getId()) {
            fileTransfer.setOrgId(GreenballMessagesUtility.getString(ba, offset, length));
         }
         else if (id == FileTransferEnum.ORG_NAME.getId()) {
            fileTransfer.setOrgName(GreenballMessagesUtility.getString(ba, offset, length));
         }
         else if (id == FileTransferEnum.USER_NAME.getId()) {
            fileTransfer.setUserName(GreenballMessagesUtility.getString(ba, offset, length));
         }
         else if (id == FileTransferEnum.CHUNK.getId()) {
            for (int index = 0; index < length; index++) {
               byte b = GreenballMessagesUtility.getByte(ba, offset * 8 + index * 8);
               fileTransfer.getChunk().add(b);
            }
         }
      }

      log4j.debug("Leaving " + METHOD);
      return fileTransfer;
   }
   // Leaving buildUnpackMethod()

   // Entered buildPackStrMethod()
   /**
    * This method packs a FileTransfer object into a String. If a required attribute is not
    * provided an exception will be thrown.
    *
    * @return A String which represents a FileTransfer message object.
    * @throws MissingAttributeException
    */
   public String packStr() throws MissingAttributeException {
      final String METHOD = "packStr()";
      log4j.debug("Entered " + METHOD);

      String msg = null;
      setErrorMsg(null);
       
      /*
       * Initialize attribute values.
       */
      InitializeFileTransfer initializeFileTransfer = new InitializeFileTransfer(this);
      Element element = initializeFileTransfer.initialize();

      if (getErrorMsg() == null) {
         /*
          * Convert object to String.
          * See convertObjectToString() in JBitByBitGenerator.java.
          */
         msg = "{" + element.getName();
         for (AbstractAttribute attr : element.getAbstractAttributes()) {
            msg = buildString(attr, msg);
         }
         msg = msg.concat("}");

         log4j.debug("msg: " + msg);
      }

      log4j.debug("Leaving " + METHOD);
      return msg;
   }
   // Leaving buildPackStrMethod()

   // Entered buildUnpackStrMethod()
   /**
    * The unpack() method unpacks a String into an object. The String must conform to the
    * SMBG grammar.
    *
     * @return The message string as an object.
    */
   public FileTransfer unpack(String buffer) {
      final String METHOD = "unpack()";
      log4j.debug("Entered " + METHOD);

      setErrorMsg(null);

      // Create the lexer
      log4j.debug("lexing...");
      SMBGLexer lexer = new SMBGLexer(new ANTLRStringStream(buffer));

      int nbrOfSyntaxErrors = lexer.getNumberOfSyntaxErrors();
      log4j.debug("# of lexer errors: " + nbrOfSyntaxErrors);

      // Check for errors
      if (nbrOfSyntaxErrors != 0) {
         for (String error : lexer.getErrors()) {
            log4j.error(error);
         }

         return null;
      }

      // Wrap a token-stream around the lexer
      CommonTokenStream tokenStream = new CommonTokenStream(lexer);

      // Create the parser
      log4j.debug("parsing...");
      SMBGParser parser = new SMBGParser(tokenStream);

      // Check for errors
      nbrOfSyntaxErrors = parser.getNumberOfSyntaxErrors();
      log4j.debug("# of parse errors: " + nbrOfSyntaxErrors);

      if (nbrOfSyntaxErrors != 0) {
         for (String error : parser.getErrors()) {
               log4j.error(error);
         }

         return null;
      }

      // Unpack the string into objects
      log4j.debug("unpacking...");
      try {
         parser.unpack();
      } catch (RecognitionException e) {
         log4j.error("Error: " + e.getMessage());
         setErrorMsg(e.getMessage());
         return null;
      }
      
      Element element = parser.getElement();
      element.dump();

      /*
       * Instantiate the FileTransfer object.
       */
       InstantiateFileTransfer instantiateFileTransfer = new InstantiateFileTransfer(element);
       FileTransfer fileTransfer = instantiateFileTransfer.instantiate();

      /*
       * Verify that a value was provided for all mandatory attributes.
       */
      String result = instantiateFileTransfer.validate();
      if (result != null) {
         log4j.error("Error: " + result);
         setErrorMsg(result);
         return null;
      }

      log4j.debug("Leaving " + METHOD);
      return fileTransfer;
   }
   // Leaving buildUnpackStrMethod()

   private String buildString(AbstractAttribute attr, String msg) {
      for (KeyValuePair kvp : attr.getKeyValuePairs()) {
         msg = msg.concat(" " + kvp.getKey() + ":" + kvp.getValue());
      }

      for (Element element : attr.getElements()) {
         msg = msg.concat(" {" + element.getName());
         for (AbstractAttribute abstractAttribute : element.getAbstractAttributes()) {
            msg = buildString(abstractAttribute, msg);
         }
         msg = msg.concat("}");
      }

      return msg;
   }

   /**
    * This method sets the fileName attribute. Setting of this value is required.
    * @param fileName The fileName attribute.
    */
   public void setFileName(String fileName) {
      this.fileName = fileName;
   }

   /**
    * This method sets the uuid attribute. Setting of this value is required.
    * @param uuid The uuid attribute.
    */
   public void setUuid(String uuid) {
      this.uuid = uuid;
   }

   /**
    * This method sets the orgId attribute. Setting of this value is required.
    * @param orgId The orgId attribute.
    */
   public void setOrgId(String orgId) {
      this.orgId = orgId;
   }

   /**
    * This method sets the orgName attribute. Setting of this value is required.
    * @param orgName The orgName attribute.
    */
   public void setOrgName(String orgName) {
      this.orgName = orgName;
   }

   /**
    * This method sets the userName attribute. Setting of this value is required.
    * @param userName The userName attribute.
    */
   public void setUserName(String userName) {
      this.userName = userName;
   }

   /**
    * This method sets the chunkNumber attribute. Setting of this value is required.
    * @param chunkNumber The chunkNumber attribute.
    */
   public void setChunkNumber(Integer chunkNumber) {
      this.chunkNumber = chunkNumber;
   }

   /**
    * This method sets the lastChunk attribute. Setting of this value is required.
    * @param lastChunk The lastChunk attribute.
    */
   public void setLastChunk(Boolean lastChunk) {
      this.lastChunk = lastChunk;
   }

   /**
    * This method sets the chunk attribute. Setting of this value is required.
    * @param chunk The chunk attribute.
    */
   public void setChunk(List<Byte> chunk) {
      this.chunk = chunk;
   }

   /**
    * This method gets the fileName attribute. The fileName attribute is a required attribute.
    */
   public String getFileName() {
      return this.fileName;
   }

   /**
    * This method gets the uuid attribute. The uuid attribute is a required attribute.
    */
   public String getUuid() {
      return this.uuid;
   }

   /**
    * This method gets the orgId attribute. The orgId attribute is a required attribute.
    */
   public String getOrgId() {
      return this.orgId;
   }

   /**
    * This method gets the orgName attribute. The orgName attribute is a required attribute.
    */
   public String getOrgName() {
      return this.orgName;
   }

   /**
    * This method gets the userName attribute. The userName attribute is a required attribute.
    */
   public String getUserName() {
      return this.userName;
   }

   /**
    * This method gets the chunkNumber attribute. The chunkNumber attribute is a required attribute.
    */
   public Integer getChunkNumber() {
      return this.chunkNumber;
   }

   /**
    * This method gets the lastChunk attribute. The lastChunk attribute is a required attribute.
    */
   public Boolean getLastChunk() {
      return this.lastChunk;
   }

   /**
    * This method gets the chunk attribute. The chunk attribute is a required attribute.
    */
   public List<Byte> getChunk() {
      if (this.chunk == null) {
         this.chunk = new ArrayList<Byte>();
      }
      return this.chunk;
   }

   /**
    * This method gets the size of the directory.
    */
   private int getFileTransferDirectorySize() {
      int directorySize = 0;

      // fileName
      directorySize++;
      // uuid
      directorySize++;
      // orgId
      directorySize++;
      // orgName
      directorySize++;
      // userName
      directorySize++;
      // chunk
      directorySize++;

      return directorySize;
   }


   /**
    * This method gets the size of the FileTransfer object.
    * @return The size in bytes.
    */
   public int getFileTransferSize() {
      int size = 0;

      int directorySize = getFileTransferDirectorySize();
      size += directorySize * GreenballMessagesDirectoryEntry.DIRECTORY_ENTRY_SIZE_IN_BYTES;
      // Add two bytes for the ID
      size += 2;
      // Add eight bytes for the CRC
      size += 8;
      // Add size of fileName
      size += (getFileName() != null ? getFileName().length() : 0);
      // Add size of uuid
      size += (getUuid() != null ? getUuid().length() : 0);
      // Add size of orgId
      size += (getOrgId() != null ? getOrgId().length() : 0);
      // Add size of orgName
      size += (getOrgName() != null ? getOrgName().length() : 0);
      // Add size of userName
      size += (getUserName() != null ? getUserName().length() : 0);
      // Add size of chunkNumber
      size += 4;
      // Add size of lastChunk
      size += 1;
      // Add size of chunk
      size += 1 * getChunk().size();

      return size;
   }

   @Override
   public String toString() {
      StringBuilder result = new StringBuilder();
      String NEW_LINE = System.getProperty("line.separator");

      result.append(this.getClass().getSimpleName() + " Object {" + NEW_LINE);
      result.append(" fileName: " + fileName + NEW_LINE);
      result.append(" uuid: " + uuid + NEW_LINE);
      result.append(" orgId: " + orgId + NEW_LINE);
      result.append(" orgName: " + orgName + NEW_LINE);
      result.append(" userName: " + userName + NEW_LINE);
      result.append(" chunkNumber: " + chunkNumber + NEW_LINE);
      result.append(" lastChunk: " + lastChunk + NEW_LINE);

      if (chunk != null) {
         int i = 0;
         for (Byte chunkByte : chunk) {
            result.append(" chunk[" + i + "]: " + chunkByte.toString() + NEW_LINE);
            i++;
         }
      }

      result.append("} ");

      return result.toString();
   }

   /**
    * The hashCode method returns this object's hashCode.
    */
   @Override
   public int hashCode() {
      final int prime = 31;
      int result = 1;
      result = prime * result
         + ((String) fileName).hashCode();
      result = prime * result
         + ((String) uuid).hashCode();
      result = prime * result
         + ((String) orgId).hashCode();
      result = prime * result
         + ((String) orgName).hashCode();
      result = prime * result
         + ((String) userName).hashCode();
      result = prime * result
         + ((Integer) chunkNumber).hashCode();
      result = prime * result
         + ((Boolean) lastChunk).hashCode();
      for (Byte b : chunk) {
         result = prime * result + b.hashCode();
      }
      return result;
   }

   /**
    * The equals method checks for equality. 
    */
   public boolean equals(Object obj) {
      if (obj == this) return true;
      if (obj == null) return false;
      if (getClass() != obj.getClass()) return false;

      FileTransfer fileTransfer = (FileTransfer) obj;
      if (!fileTransfer.getFileName().equals(this.getFileName())) {
         return false;
      }

      if (!fileTransfer.getUuid().equals(this.getUuid())) {
         return false;
      }

      if (!fileTransfer.getOrgId().equals(this.getOrgId())) {
         return false;
      }

      if (!fileTransfer.getOrgName().equals(this.getOrgName())) {
         return false;
      }

      if (!fileTransfer.getUserName().equals(this.getUserName())) {
         return false;
      }

      if (fileTransfer.getChunkNumber() != this.getChunkNumber()) {
         return false;
      }

      if (fileTransfer.getLastChunk() != this.getLastChunk()) {
         return false;
      }

      if (fileTransfer.getChunk() != this.getChunk()) {
         return false;
      }

      return true;
   }
}
