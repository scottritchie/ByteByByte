/*
 * WARNING: This file was generated by BitByBitGenerator.xtend (see
 * generateInstantiationFile()). 
 * Do not edit.
 */
package com.viacron.greenball.app.bbb;
import java.util.ArrayList;
import java.util.List;

import org.apache.log4j.Logger;

import com.viacron.utils.StringMessageBuffer.parser.AbstractAttribute;
import com.viacron.utils.StringMessageBuffer.parser.Element;
import com.viacron.utils.StringMessageBuffer.parser.KeyValuePair;

public class InstantiateRequestSubscriptionPhase2 {
   private static Logger log4j = Logger.getLogger(InstantiateRequestSubscriptionPhase2.class);

   /*
    * The isUpdated flags. If an attribute is required by the grammar then a value
    * must be provided, and the isUpdated flag will be set when the value is
    * obtained during unpacking. After the object is constructed during unpacking, 
    * validation will be performed to make sure a value was provided for all attributes
    * that are not optional. 
    * See buildFlags() in JBitByBitGenerator.java.
    */
   private boolean isRequestorUpdated = false;
   private boolean isOrgIdUpdated = false;
   private boolean isOrgNameUpdated = false;
   private boolean isSessionIdUpdated = false;

   private Element element;
   private RequestSubscriptionPhase2 requestSubscriptionPhase2;

   /**
    * The InstantiateRequestSubscriptionPhase2 class instantiates the RequestSubscriptionPhase2 object.
    *
    * @param element The root element returned by the parser after parsing the string
    *                representing the RequestSubscriptionPhase2.
    */
   public InstantiateRequestSubscriptionPhase2(Element element) {
      this.element = element;   
      this.requestSubscriptionPhase2 = new RequestSubscriptionPhase2();
   }

   /**
    * This method instantiates the RequestSubscriptionPhase2.
    *
    * @return The RequestSubscriptionPhase2.
    */
   public RequestSubscriptionPhase2 instantiate() {
      final String METHOD = "instantiate()";
      log4j.debug("Entered " + METHOD);

      log4j.debug("RequestSubscriptionPhase2: " + element.getName());
      log4j.debug("# of attributes: " + element.getAbstractAttributes().size());

      instantiateRequestSubscriptionPhase2Attributes(element);

      for (AbstractAttribute abstractAttribute : element
            .getAbstractAttributes()) {
         for (Element e : abstractAttribute.getElements()) {
            log4j.debug("element name: " + e.getName());
      instantiateRequestSubscriptionPhase2SubTypeRefs(e);
      }
      }

      log4j.debug("Leaving " + METHOD);
      return requestSubscriptionPhase2;
      }

      /*
       * This method instantiates the RequestSubscriptionPhase2 attributes.
       * 
       * @param Element element
       */
      private void instantiateRequestSubscriptionPhase2Attributes(Element element) {
      final String METHOD = "instantiateRequestSubscriptionPhase2Attributes()";
      log4j.debug("Entered " + METHOD);

      log4j.debug("element name: " + element.getName());
      for (AbstractAttribute attr : element.getAbstractAttributes()) {
         for (KeyValuePair kvp : attr.getKeyValuePairs()) {
            log4j.debug("key: " + kvp.getKey());
            log4j.debug("value: " + kvp.getValue());
            // Entered instantiateMsgAttribute()
            if (kvp.getKey().equals("requestor")) {
               log4j.debug("setting value of \"requestor\" to: " + kvp.getValue());
               this.isRequestorUpdated = true;
               requestSubscriptionPhase2.setRequestor(kvp.getValue());
            }
            // Leaving instantiateMsgAttribute()
            // Entered instantiateMsgAttribute()
            if (kvp.getKey().equals("message")) {
               log4j.debug("setting value of \"message\" to: " + kvp.getValue());
               requestSubscriptionPhase2.setMessage(kvp.getValue());
            }
            // Leaving instantiateMsgAttribute()
            // Entered instantiateMsgAttribute()
            if (kvp.getKey().equals("orgId")) {
               log4j.debug("setting value of \"orgId\" to: " + kvp.getValue());
               this.isOrgIdUpdated = true;
               requestSubscriptionPhase2.setOrgId(kvp.getValue());
            }
            // Leaving instantiateMsgAttribute()
            // Entered instantiateMsgAttribute()
            if (kvp.getKey().equals("orgName")) {
               log4j.debug("setting value of \"orgName\" to: " + kvp.getValue());
               this.isOrgNameUpdated = true;
               requestSubscriptionPhase2.setOrgName(kvp.getValue());
            }
            // Leaving instantiateMsgAttribute()
            // Entered instantiateMsgAttribute()
            if (kvp.getKey().equals("sessionId")) {
               log4j.debug("setting value of \"sessionId\" to: " + kvp.getValue());
               this.isSessionIdUpdated = true;
               requestSubscriptionPhase2.setSessionId(kvp.getValue());
            }
            // Leaving instantiateMsgAttribute()
         }
      }

      log4j.debug("Leaving " + METHOD);
   }

   /*
    * This method instantiates the RequestSubscriptionPhase2 SubType references.
    * 
    * @param Element element
    */
   private void instantiateRequestSubscriptionPhase2SubTypeRefs(Element element) {
      final String METHOD = "instantiateRequestSubscriptionPhase2SubTypeRefs()";
      log4j.debug("Entered " + METHOD);


      log4j.debug("Leaving " + METHOD);
   }


   /**
    * The validate method checks that a value was provided for all required 
    * attributes.
    *
    * @return The error message or null if no error occurred.
    */
   protected String validate() {
      final String METHOD = "validate()";
      log4j.debug("Entered " + METHOD);

      String result = null;

      // Entered buildValidationMethod()
      if (!this.isRequestorUpdated) {
         String msg = "Requestor is required, but no value was provided.";
         if (result == null) {
            result = msg;
         }
      }

      if (!this.isOrgIdUpdated) {
         String msg = "OrgId is required, but no value was provided.";
         if (result == null) {
            result = msg;
         }
         else {
            result = result.concat("\n" + msg);
         }
      }

      if (!this.isOrgNameUpdated) {
         String msg = "OrgName is required, but no value was provided.";
         if (result == null) {
            result = msg;
         }
         else {
            result = result.concat("\n" + msg);
         }
      }

      if (!this.isSessionIdUpdated) {
         String msg = "SessionId is required, but no value was provided.";
         if (result == null) {
            result = msg;
         }
         else {
            result = result.concat("\n" + msg);
         }
      }


      if (result != null) {
         log4j.debug("result: " + result);
      }

      log4j.debug("Leaving " + METHOD);
      return result;
   }
}
