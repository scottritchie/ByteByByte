/*
 * WARNING: This file was generated by BitByBitGenerator.xtend (see
 * generateInstantiationFile()). 
 * Do not edit.
 */
package com.viacron.greenball.app.bbb;
import java.util.ArrayList;
import java.util.List;

import org.apache.log4j.Logger;

import com.viacron.utils.StringMessageBuffer.parser.AbstractAttribute;
import com.viacron.utils.StringMessageBuffer.parser.Element;
import com.viacron.utils.StringMessageBuffer.parser.KeyValuePair;

public class InstantiateSubscribers {
   private static Logger log4j = Logger.getLogger(InstantiateSubscribers.class);

   /*
    * The isUpdated flags. If an attribute is required by the grammar then a value
    * must be provided, and the isUpdated flag will be set when the value is
    * obtained during unpacking. After the object is constructed during unpacking, 
    * validation will be performed to make sure a value was provided for all attributes
    * that are not optional. 
    * See buildFlags() in JBitByBitGenerator.java.
    */
   private boolean isOrganizationUpdated = false;
   private boolean isUsersUpdated = false;
   private boolean isSessionIdUpdated = false;

   private Element element;
   private Subscribers subscribers;

   /**
    * The InstantiateSubscribers class instantiates the Subscribers object.
    *
    * @param element The root element returned by the parser after parsing the string
    *                representing the Subscribers.
    */
   public InstantiateSubscribers(Element element) {
      this.element = element;   
      this.subscribers = new Subscribers();
   }

   /**
    * This method instantiates the Subscribers.
    *
    * @return The Subscribers.
    */
   public Subscribers instantiate() {
      final String METHOD = "instantiate()";
      log4j.debug("Entered " + METHOD);

      log4j.debug("Subscribers: " + element.getName());
      log4j.debug("# of attributes: " + element.getAbstractAttributes().size());

      instantiateSubscribersAttributes(element);

      for (AbstractAttribute abstractAttribute : element
            .getAbstractAttributes()) {
         for (Element e : abstractAttribute.getElements()) {
            log4j.debug("element name: " + e.getName());
      instantiateSubscribersSubTypeRefs(e);
      }
      }

      log4j.debug("Leaving " + METHOD);
      return subscribers;
      }

      /*
       * This method instantiates the Subscribers attributes.
       * 
       * @param Element element
       */
      private void instantiateSubscribersAttributes(Element element) {
      final String METHOD = "instantiateSubscribersAttributes()";
      log4j.debug("Entered " + METHOD);

      log4j.debug("element name: " + element.getName());
      for (AbstractAttribute attr : element.getAbstractAttributes()) {
         for (KeyValuePair kvp : attr.getKeyValuePairs()) {
            log4j.debug("key: " + kvp.getKey());
            log4j.debug("value: " + kvp.getValue());
            // Entered instantiateMsgAttribute()
            if (kvp.getKey().equals("organization")) {
               log4j.debug("setting value of \"organization\" to: " + kvp.getValue());
               this.isOrganizationUpdated = true;
               subscribers.setOrganization(kvp.getValue());
            }
            // Leaving instantiateMsgAttribute()
            // Entered instantiateMsgAttribute()
            if (kvp.getKey().equals("sessionId")) {
               log4j.debug("setting value of \"sessionId\" to: " + kvp.getValue());
               this.isSessionIdUpdated = true;
               subscribers.setSessionId(kvp.getValue());
            }
            // Leaving instantiateMsgAttribute()
         }
      }

      log4j.debug("Leaving " + METHOD);
   }

   /*
    * This method instantiates the Subscribers SubType references.
    * 
    * @param Element element
    */
   private void instantiateSubscribersSubTypeRefs(Element element) {
      final String METHOD = "instantiateSubscribersSubTypeRefs()";
      log4j.debug("Entered " + METHOD);

      // Entered instantiateMessageSubTypeRefList()
      if (element.getName().startsWith("users[")) {
         log4j.debug("found element: users");
         this.isUsersUpdated = true;
         instantiateUsers(element);
         return;
      }
      // Leaving instantiateMessageSubTypeRefList()


      log4j.debug("Leaving " + METHOD);
   }

/*
 * This method instantiates the users attribute which is a list of UserSubType
 * @param Element element
 */
private void instantiateUsers(Element element) {
      // Entered instantiateSubTypeRefList()
   final String METHOD = "instantiateUsers()";
   log4j.debug("Entered " + METHOD);

   if (subscribers.getUsers() == null) {
   subscribers.setUsers(new ArrayList<UserSubType>());
   }

   InstantiateUserSubType instantiateUserSubType = new InstantiateUserSubType(element);
   UserSubType userSubType = instantiateUserSubType.instantiate();
   subscribers.getUsers().add(userSubType);
   log4j.debug("Leaving " + METHOD);
   // Leaving instantiateSubTypeRefList()
}

   /**
    * The validate method checks that a value was provided for all required 
    * attributes.
    *
    * @return The error message or null if no error occurred.
    */
   protected String validate() {
      final String METHOD = "validate()";
      log4j.debug("Entered " + METHOD);

      String result = null;

      // Entered buildValidationMethod()
      if (!this.isOrganizationUpdated) {
         String msg = "Organization is required, but no value was provided.";
         if (result == null) {
            result = msg;
         }
      }

      if (!this.isUsersUpdated) {
         String msg = "Users is required, but no value was provided.";
         if (result == null) {
            result = msg;
         }
         else {
            result = result.concat("\n" + msg);
         }
      }

      if (!this.isSessionIdUpdated) {
         String msg = "SessionId is required, but no value was provided.";
         if (result == null) {
            result = msg;
         }
         else {
            result = result.concat("\n" + msg);
         }
      }


      if (result != null) {
         log4j.debug("result: " + result);
      }

      log4j.debug("Leaving " + METHOD);
      return result;
   }
}
