/*
 * WARNING: This file was generated by BitByBitGenerator.xtend (see
 * generateInstantiationFile()). 
 * Do not edit.
 */
package com.viacron.greenball.app.bbb;
import java.util.ArrayList;
import java.util.List;

import org.apache.log4j.Logger;

import com.viacron.utils.StringMessageBuffer.parser.AbstractAttribute;
import com.viacron.utils.StringMessageBuffer.parser.Element;
import com.viacron.utils.StringMessageBuffer.parser.KeyValuePair;

public class InstantiateTopicSubscriptionRequest {
   private static Logger log4j = Logger.getLogger(InstantiateTopicSubscriptionRequest.class);

   /*
    * The isUpdated flags. If an attribute is required by the grammar then a value
    * must be provided, and the isUpdated flag will be set when the value is
    * obtained during unpacking. After the object is constructed during unpacking, 
    * validation will be performed to make sure a value was provided for all attributes
    * that are not optional. 
    * See buildFlags() in JBitByBitGenerator.java.
    */
   private boolean isTopicsUpdated = false;

   private Element element;
   private TopicSubscriptionRequest topicSubscriptionRequest;

   /**
    * The InstantiateTopicSubscriptionRequest class instantiates the TopicSubscriptionRequest object.
    *
    * @param element The root element returned by the parser after parsing the string
    *                representing the TopicSubscriptionRequest.
    */
   public InstantiateTopicSubscriptionRequest(Element element) {
      this.element = element;   
      this.topicSubscriptionRequest = new TopicSubscriptionRequest();
   }

   /**
    * This method instantiates the TopicSubscriptionRequest.
    *
    * @return The TopicSubscriptionRequest.
    */
   public TopicSubscriptionRequest instantiate() {
      final String METHOD = "instantiate()";
      log4j.debug("Entered " + METHOD);

      log4j.debug("TopicSubscriptionRequest: " + element.getName());
      log4j.debug("# of attributes: " + element.getAbstractAttributes().size());

      instantiateTopicSubscriptionRequestAttributes(element);

      for (AbstractAttribute abstractAttribute : element
            .getAbstractAttributes()) {
         for (Element e : abstractAttribute.getElements()) {
            log4j.debug("element name: " + e.getName());
      instantiateTopicSubscriptionRequestSubTypeRefs(e);
      }
      }

      log4j.debug("Leaving " + METHOD);
      return topicSubscriptionRequest;
      }

      /*
       * This method instantiates the TopicSubscriptionRequest attributes.
       * 
       * @param Element element
       */
      private void instantiateTopicSubscriptionRequestAttributes(Element element) {
      final String METHOD = "instantiateTopicSubscriptionRequestAttributes()";
      log4j.debug("Entered " + METHOD);

      log4j.debug("element name: " + element.getName());
      for (AbstractAttribute attr : element.getAbstractAttributes()) {
         for (KeyValuePair kvp : attr.getKeyValuePairs()) {
            log4j.debug("key: " + kvp.getKey());
            log4j.debug("value: " + kvp.getValue());
            // Entered instantiateMsgAttributeList()
            if (kvp.getKey().startsWith("topics[")) {
               this.isTopicsUpdated = true;
               if(topicSubscriptionRequest.getTopics() == null) {
               topicSubscriptionRequest.setTopics(new ArrayList<String>());
               }
               topicSubscriptionRequest.getTopics().add(kvp.getValue());
            }
            // Leaving instantiateMsgAttributeList()
         }
      }

      log4j.debug("Leaving " + METHOD);
   }

   /*
    * This method instantiates the TopicSubscriptionRequest SubType references.
    * 
    * @param Element element
    */
   private void instantiateTopicSubscriptionRequestSubTypeRefs(Element element) {
      final String METHOD = "instantiateTopicSubscriptionRequestSubTypeRefs()";
      log4j.debug("Entered " + METHOD);


      log4j.debug("Leaving " + METHOD);
   }


   /**
    * The validate method checks that a value was provided for all required 
    * attributes.
    *
    * @return The error message or null if no error occurred.
    */
   protected String validate() {
      final String METHOD = "validate()";
      log4j.debug("Entered " + METHOD);

      String result = null;

      // Entered buildValidationMethod()
      if (!this.isTopicsUpdated) {
         String msg = "Topics is required, but no value was provided.";
         if (result == null) {
            result = msg;
         }
      }


      if (result != null) {
         log4j.debug("result: " + result);
      }

      log4j.debug("Leaving " + METHOD);
      return result;
   }
}
