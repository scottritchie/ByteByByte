/*
 * WARNING: This file was generated by BitByBitGenerator.xtend (see
 * generateSubTypeInstantiationFile()).
 * Do not edit.
 */
package com.viacron.greenball.app.bbb;
import java.util.ArrayList;
import java.util.List;

import org.apache.log4j.Logger;

import com.viacron.utils.StringMessageBuffer.parser.AbstractAttribute;
import com.viacron.utils.StringMessageBuffer.parser.Element;
import com.viacron.utils.StringMessageBuffer.parser.KeyValuePair;

public class InstantiateScopeIdType {
private static Logger log4j = Logger.getLogger(InstantiateScopeIdType.class);

private Element element;
private ScopeIdType scopeIdType;

/*
 * The isUpdated flags. If an attribute is required by the grammar then a value
 * must be provided, and the isUpdated flag will be set when the value is
 * obtained during unpacking. After the object is constructed during unpacking, 
 * validation will be performed to make sure a value was provided for all attributes
 * that are not optional. 
 * See buildFlags() in JBitByBitGenerator.java.
 */
private boolean isIdUpdated = false;
private boolean isSmsMessageSentUpdated = false;
private boolean isEmailMessageSentUpdated = false;

/**
 * This method constructs a InstantiateScopeIdType object based on an Element
 * object.
 * 
 * @param element
 *            The Element.
 */
public InstantiateScopeIdType(Element element) {
this.element = element;
this.scopeIdType = new ScopeIdType();
}

/**
 * The instantiate method instantiates a ScopeIdType object.
 * 
 * @return The ScopeIdType.
 */
protected ScopeIdType instantiate() {
final String METHOD = "instantiate()";
log4j.debug("Entered " + METHOD);

log4j.debug("element: " + element.getName());

for (AbstractAttribute abstractAttribute : element.getAbstractAttributes()) {
      // Entered instantiateSubTypeElements()
   // Leaving instantiateSubTypeElements()


   for (KeyValuePair kvp : abstractAttribute.getKeyValuePairs()) {
         // Entered instantiateSubTypeAttributes()
   if (kvp.getKey().equals("id")) {
    this.isIdUpdated = true;
   log4j.debug("setting value of \"id\" to: " + kvp.getValue());
   scopeIdType.setId(kvp.getValue());
   }
   else if (kvp.getKey().equals("smsMessageSent")) {
    this.isSmsMessageSentUpdated = true;
   log4j.debug("setting value of \"smsMessageSent\" to: " + kvp.getValue());
   scopeIdType.setSmsMessageSent(kvp.getValue().equals("true"));
   }
   else if (kvp.getKey().equals("emailMessageSent")) {
    this.isEmailMessageSentUpdated = true;
   log4j.debug("setting value of \"emailMessageSent\" to: " + kvp.getValue());
   scopeIdType.setEmailMessageSent(kvp.getValue().equals("true"));
   }
   // Leaving instantiateSubTypeAttributes()

   }
}

log4j.debug("Leaving " + METHOD);
return scopeIdType;
}

/**
 * The validate method checks that a value was provided for all required 
 * attributes.
 *
 * @return The error message or null if no error occurred.
 */
protected String validate() {
final String METHOD = "validate()";
log4j.debug("Entered " + METHOD);

String result = null;

// Entered buildValidationMethod()
if (!this.isIdUpdated) {
   String msg = "Id is required, but no value was provided.";
   if (result == null) {
      result = msg;
   }
}

if (!this.isSmsMessageSentUpdated) {
   String msg = "SmsMessageSent is required, but no value was provided.";
   if (result == null) {
      result = msg;
   }
   else {
      result = result.concat("\n" + msg);
   }
}

if (!this.isEmailMessageSentUpdated) {
   String msg = "EmailMessageSent is required, but no value was provided.";
   if (result == null) {
      result = msg;
   }
   else {
      result = result.concat("\n" + msg);
   }
}


if (result != null) {
   log4j.debug("result: " + result);
}

log4j.debug("Leaving " + METHOD);
return result;
}
}
