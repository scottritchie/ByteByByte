/*
 * WARNING: This file was generated by BitByBitGenerator.xtend (see
 * generateClassFile()). 
 * Do not edit.
 */
package com.viacron.greenball.app.bbb;

import java.nio.charset.Charset;
import java.util.ArrayList;
import java.util.EnumSet;
import java.util.HashMap;
import java.util.List;
import java.util.Map;

import org.apache.log4j.Logger;
/**
 * The ScopeIdType class is a SubType referenced by message objects defined
 * by the GreenballMessages grammar.
 */
public class ScopeIdType {
   private static final int ID = GreenballMessagesEnum.SCOPE_ID_TYPE.getId();

   /**
    * The number of bits required to represent the ScopeIdTypeEnum IDs.
    * # of attributes: 3
    * # of PEnumRefs: 0
    */
   private static final int ID_BITS = 2;

   private static GreenballMessagesDirectoryEntry[] theDirectory;
   private static final int DIRECTORY_OFFSET = 2;

   private static Logger log4j = Logger.getLogger(ScopeIdType.class);

   // Entered buildClassEnum()
   private enum ScopeIdTypeEnum {
      // This is a directory item. It's offset and length cannot be determined
      // until runtime.
      ID(0, -1, 0, true),

      // sizeInBits: 8
      SMS_MESSAGE_SENT(1, 0, 0, false),

      // sizeInBits: 8
      EMAIL_MESSAGE_SENT(2, 8, 0, false);

      private final int id;
      private final int offsetInBits;
      private int length;
      private boolean isDirectoryEntry;

      ScopeIdTypeEnum(int id, int offsetInBits, int length, boolean isDirectoryEntry) {
         this.id = id;
         this.offsetInBits = offsetInBits;
         this.length = length;
         this.isDirectoryEntry = isDirectoryEntry;
      }

      public int getId() {
         return this.id;
      }

      public int getOffsetInBits() {
         return this.offsetInBits;
      }

      public int getLength() {
         return this.length;
      }

      public void setLength(int length) {
         this.length = length;
      }
   } // Leaving buildClassEnum()

   /*
    * The instance variables.
    * See buildInstanceVariables() in JBitByBitGenerator.java.
    */
   private String id;
   private boolean smsMessageSent;
   private boolean emailMessageSent;

   private String theErrorMsg;

   /**
    * The ScopeIdType no-arg constructor.
    */
   public ScopeIdType() {}

   /**
    * The ScopeIdType constructor.
    * See buildParameterAnnotations() in JBitByBitGenerator.java.
    *
    * @param id The id message attribute.
    * @param smsMessageSent The smsMessageSent message attribute.
    * @param emailMessageSent The emailMessageSent message attribute.
    */
   public ScopeIdType(String id,
                      Boolean smsMessageSent,
                      Boolean emailMessageSent) {
      // Entered buildParameterAssignments()
      this.setId(id);
      this.setSmsMessageSent(smsMessageSent);
      this.setEmailMessageSent(emailMessageSent);
      // Leaving buildParameterAssignments()
   }

   /**
    * The ScopeIdType copy constructor.
    */
   public ScopeIdType(ScopeIdType scopeIdType) {
      // Entered buildCopyConstructorParameterAssignments()
      this.setId(scopeIdType.getId());
      this.setSmsMessageSent(scopeIdType.getSmsMessageSent());
      this.setEmailMessageSent(scopeIdType.getEmailMessageSent());
      // Leaving buildCopyConstructorParameterAssignments()
   }

   // Entered buildSetErrorMsgMethod()
   private void setErrorMsg(String errorMsg) {
      theErrorMsg = errorMsg;
   }
   // Leaving buildSetErrorMsgMethod()

   // Entered buildGetErrorMsgMethod()
   /**
    * This method returns the error message or null if no error
    * occurred.
    */
   public String getErrorMsg() {
      return theErrorMsg;
   }
   // Leaving buildGetErrorMsgMethod()

   // Entered buildPackMethod()
   /**
    * This method packs a ScopeIdType object into a byte array. If a required attribute is not
    * provided an exception will be thrown.
    *
    * @return A byte array which represents a ScopeIdType message object.
    * @throws MissingAttributeException
    */
   public byte[] pack() throws MissingAttributeException {
      final String METHOD = "pack()";
      log4j.debug("Entered " + METHOD);

      int byteArraySize = getScopeIdTypeSize();
      log4j.debug("byteArraySize: " + byteArraySize);
      byte[] bb = new byte[byteArraySize];

      /*
       * Convert object to byte array.
       */
      int offset = 0;

      if (this.getSmsMessageSent() != null) {
         log4j.debug(String.format("inserting %s at offset %d", "smsMessageSent", offset));
         bb = GreenballMessagesUtility.insertBitfield(bb, offset * 8, 1, this.getSmsMessageSent() == true ? 1 : 0);
         offset += 1;
      } else {
         /*
          * Missing required attribute, generate an error message and throw
          * an exception.
          */
         setErrorMsg("Error packing object. The attribute \"smsMessageSent\" is required, but no value was provided.");
         log4j.error(getErrorMsg());
         throw new MissingAttributeException(getErrorMsg());
      }

      if (this.getEmailMessageSent() != null) {
         log4j.debug(String.format("inserting %s at offset %d", "emailMessageSent", offset));
         bb = GreenballMessagesUtility.insertBitfield(bb, offset * 8, 1, this.getEmailMessageSent() == true ? 1 : 0);
         offset += 1;
      } else {
         /*
          * Missing required attribute, generate an error message and throw
          * an exception.
          */
         setErrorMsg("Error packing object. The attribute \"emailMessageSent\" is required, but no value was provided.");
         log4j.error(getErrorMsg());
         throw new MissingAttributeException(getErrorMsg());
      }


      ScopeIdTypeEnum.ID.setLength(this.getId() != null ? this.getId().length() : 0);

      int directorySize = getScopeIdTypeDirectorySize();
      if (directorySize != 0) {
         // Create the directory
         theDirectory = new GreenballMessagesDirectoryEntry[directorySize];
         for (int i = 0; i < directorySize; i++) {
            theDirectory[i] = new GreenballMessagesDirectoryEntry();
         }

         // Fill the directory
         int index = 0;
         offset = DIRECTORY_OFFSET
               + GreenballMessagesDirectoryEntry.DIRECTORY_ENTRY_SIZE_IN_BYTES
               * theDirectory.length;

         for (ScopeIdTypeEnum scopeIdTypeEnum : ScopeIdTypeEnum.values()) {
            if (scopeIdTypeEnum.isDirectoryEntry == true) {
               int id = scopeIdTypeEnum.getId();
               int length = scopeIdTypeEnum.getLength();

               theDirectory[index].setId(id);
               theDirectory[index].setLength(length);
               theDirectory[index].setOffset(offset);

               // Insert the data
               if (id == ScopeIdTypeEnum.ID.getId()) {
                  log4j.debug(String.format("inserting %s at offset %d", "id", offset));
                  if (getId() != null) {
                     bb = GreenballMessagesUtility.insertString(bb, offset, getId());
                     offset += length * 1;
                  }
               }
               index++;
            }
         }

         // Insert the directory
         log4j.debug(String.format("inserting the directory at offset %d", DIRECTORY_OFFSET));
         offset = DIRECTORY_OFFSET;
         index = 0;
         for (GreenballMessagesDirectoryEntry directoryEntry : theDirectory) {
            log4j.debug(String.format("index[%d]: ", index));
            log4j.debug(String.format("  ID:     %d", directoryEntry.getId()));
            log4j.debug(String.format("  length: %d", directoryEntry.getLength()));
            log4j.debug(String.format("  offset: %d", directoryEntry.getOffset()));
            bb = GreenballMessagesUtility.insertInteger(bb, offset, directoryEntry.getId());
            bb = GreenballMessagesUtility.insertInteger(bb, offset + 4, directoryEntry.getLength());
            bb = GreenballMessagesUtility.insertInteger(bb, offset + 8, directoryEntry.getOffset());

            offset += GreenballMessagesDirectoryEntry.DIRECTORY_ENTRY_SIZE_IN_BYTES;
            index++;
         }

      }
      log4j.debug("Leaving " + METHOD);
      return bb;
   }
   // Leaving buildPackMethod()

   // Entered buildUnpackMethod()
   /**
    * This method unpacks a byte array containing a ScopeIdType object.
    *
    * @return The byte array as a ScopeIdType message object.
    */
   public ScopeIdType unpack(byte[] ba) {
      final String METHOD = "unpack()";
      log4j.debug("Entered " + METHOD);

      ScopeIdType scopeIdType = new ScopeIdType();

      scopeIdType.setSmsMessageSent(GreenballMessagesUtility.getBoolean(ba, ScopeIdTypeEnum.SMS_MESSAGE_SENT.getOffsetInBits()));
      scopeIdType.setEmailMessageSent(GreenballMessagesUtility.getBoolean(ba, ScopeIdTypeEnum.EMAIL_MESSAGE_SENT.getOffsetInBits()));

      // Get the directory
      int directorySize = getScopeIdTypeDirectorySize();
      theDirectory = new GreenballMessagesDirectoryEntry[directorySize];
      int dirOffset = DIRECTORY_OFFSET;

      for (int entry = 0 ; entry < directorySize; entry++) {
         GreenballMessagesDirectoryEntry directoryEntry = new GreenballMessagesDirectoryEntry();

         int id = GreenballMessagesUtility.getInteger(ba, dirOffset * 8);
         directoryEntry.setId(id);
         int length = GreenballMessagesUtility.getInteger(ba, (dirOffset + 4) * 8);
         directoryEntry.setLength(length);
         int offset = GreenballMessagesUtility.getInteger(ba, (dirOffset + 8) * 8);
         directoryEntry.setOffset(offset);

         theDirectory[entry] = directoryEntry;

         dirOffset += GreenballMessagesDirectoryEntry.DIRECTORY_ENTRY_SIZE_IN_BYTES;
      }

      for (GreenballMessagesDirectoryEntry directoryEntry : theDirectory) {
         int id = directoryEntry.getId();
         int offset = directoryEntry.getOffset();
         int length = directoryEntry.getLength();

         if (id == ScopeIdTypeEnum.ID.getId()) {
            scopeIdType.setId(GreenballMessagesUtility.getString(ba, offset, length));
         }
      }

      log4j.debug("Leaving " + METHOD);
      return scopeIdType;
   }
   // Leaving buildUnpackMethod()

   /**
    * This method sets the id attribute. Setting of this value is required.
    * @param id The id attribute.
    */
   public void setId(String id) {
      this.id = id;
   }

   /**
    * This method sets the smsMessageSent attribute. Setting of this value is required.
    * @param smsMessageSent The smsMessageSent attribute.
    */
   public void setSmsMessageSent(Boolean smsMessageSent) {
      this.smsMessageSent = smsMessageSent;
   }

   /**
    * This method sets the emailMessageSent attribute. Setting of this value is required.
    * @param emailMessageSent The emailMessageSent attribute.
    */
   public void setEmailMessageSent(Boolean emailMessageSent) {
      this.emailMessageSent = emailMessageSent;
   }

   /**
    * This method gets the id attribute. The id attribute is a required attribute.
    */
   public String getId() {
      return this.id;
   }

   /**
    * This method gets the smsMessageSent attribute. The smsMessageSent attribute is a required attribute.
    */
   public Boolean getSmsMessageSent() {
      return this.smsMessageSent;
   }

   /**
    * This method gets the emailMessageSent attribute. The emailMessageSent attribute is a required attribute.
    */
   public Boolean getEmailMessageSent() {
      return this.emailMessageSent;
   }

   /**
    * This method gets the size of the directory.
    */
   private int getScopeIdTypeDirectorySize() {
      int directorySize = 0;

      // id
      directorySize++;

      return directorySize;
   }


   /**
    * This method gets the size of the ScopeIdType object.
    * @return The size in bytes.
    */
   public int getScopeIdTypeSize() {
      int size = 0;

      int directorySize = getScopeIdTypeDirectorySize();
      size += directorySize * GreenballMessagesDirectoryEntry.DIRECTORY_ENTRY_SIZE_IN_BYTES;
      // Add size of id
      size += (getId() != null ? getId().length() : 0);
      // Add size of smsMessageSent
      size += 1;
      // Add size of emailMessageSent
      size += 1;

      return size;
   }

   @Override
   public String toString() {
      StringBuilder result = new StringBuilder();
      String NEW_LINE = System.getProperty("line.separator");

      result.append(this.getClass().getSimpleName() + " Object {" + NEW_LINE);
      result.append(" id: " + id + NEW_LINE);
      result.append(" smsMessageSent: " + smsMessageSent + NEW_LINE);
      result.append(" emailMessageSent: " + emailMessageSent + NEW_LINE);

      result.append("} ");

      return result.toString();
   }

   /**
    * The hashCode method returns this object's hashCode.
    */
   @Override
   public int hashCode() {
      final int prime = 31;
      int result = 1;
      result = prime * result
         + ((String) id).hashCode();
      result = prime * result
         + ((Boolean) smsMessageSent).hashCode();
      result = prime * result
         + ((Boolean) emailMessageSent).hashCode();
      return result;
   }

   /**
    * The equals method checks for equality. 
    */
   public boolean equals(Object obj) {
      if (obj == this) return true;
      if (obj == null) return false;
      if (getClass() != obj.getClass()) return false;

      ScopeIdType scopeIdType = (ScopeIdType) obj;
      if (!scopeIdType.getId().equals(this.getId())) {
         return false;
      }

      if (scopeIdType.getSmsMessageSent() != this.getSmsMessageSent()) {
         return false;
      }

      if (scopeIdType.getEmailMessageSent() != this.getEmailMessageSent()) {
         return false;
      }

      return true;
   }
}
