/*
 * WARNING: This file was generated by BitByBitGenerator.xtend (see
 * generateInstantiationFile()). 
 * Do not edit.
 */
package com.viacron.greenball.app.bbb;
import java.util.ArrayList;
import java.util.List;

import org.apache.log4j.Logger;

import com.viacron.utils.StringMessageBuffer.parser.AbstractAttribute;
import com.viacron.utils.StringMessageBuffer.parser.Element;
import com.viacron.utils.StringMessageBuffer.parser.KeyValuePair;

public class InstantiateSubscriptionRequests {
   private static Logger log4j = Logger.getLogger(InstantiateSubscriptionRequests.class);

   /*
    * The isUpdated flags. If an attribute is required by the grammar then a value
    * must be provided, and the isUpdated flag will be set when the value is
    * obtained during unpacking. After the object is constructed during unpacking, 
    * validation will be performed to make sure a value was provided for all attributes
    * that are not optional. 
    * See buildFlags() in JBitByBitGenerator.java.
    */
   private boolean isRequestsUpdated = false;
   private boolean isSessionIdUpdated = false;

   private Element element;
   private SubscriptionRequests subscriptionRequests;

   /**
    * The InstantiateSubscriptionRequests class instantiates the SubscriptionRequests object.
    *
    * @param element The root element returned by the parser after parsing the string
    *                representing the SubscriptionRequests.
    */
   public InstantiateSubscriptionRequests(Element element) {
      this.element = element;   
      this.subscriptionRequests = new SubscriptionRequests();
   }

   /**
    * This method instantiates the SubscriptionRequests.
    *
    * @return The SubscriptionRequests.
    */
   public SubscriptionRequests instantiate() {
      final String METHOD = "instantiate()";
      log4j.debug("Entered " + METHOD);

      log4j.debug("SubscriptionRequests: " + element.getName());
      log4j.debug("# of attributes: " + element.getAbstractAttributes().size());

      instantiateSubscriptionRequestsAttributes(element);

      for (AbstractAttribute abstractAttribute : element
            .getAbstractAttributes()) {
         for (Element e : abstractAttribute.getElements()) {
            log4j.debug("element name: " + e.getName());
      instantiateSubscriptionRequestsSubTypeRefs(e);
      }
      }

      log4j.debug("Leaving " + METHOD);
      return subscriptionRequests;
      }

      /*
       * This method instantiates the SubscriptionRequests attributes.
       * 
       * @param Element element
       */
      private void instantiateSubscriptionRequestsAttributes(Element element) {
      final String METHOD = "instantiateSubscriptionRequestsAttributes()";
      log4j.debug("Entered " + METHOD);

      log4j.debug("element name: " + element.getName());
      for (AbstractAttribute attr : element.getAbstractAttributes()) {
         for (KeyValuePair kvp : attr.getKeyValuePairs()) {
            log4j.debug("key: " + kvp.getKey());
            log4j.debug("value: " + kvp.getValue());
            // Entered instantiateMsgAttribute()
            if (kvp.getKey().equals("sessionId")) {
               log4j.debug("setting value of \"sessionId\" to: " + kvp.getValue());
               this.isSessionIdUpdated = true;
               subscriptionRequests.setSessionId(kvp.getValue());
            }
            // Leaving instantiateMsgAttribute()
         }
      }

      log4j.debug("Leaving " + METHOD);
   }

   /*
    * This method instantiates the SubscriptionRequests SubType references.
    * 
    * @param Element element
    */
   private void instantiateSubscriptionRequestsSubTypeRefs(Element element) {
      final String METHOD = "instantiateSubscriptionRequestsSubTypeRefs()";
      log4j.debug("Entered " + METHOD);

      // Entered instantiateMessageSubTypeRefList()
      if (element.getName().startsWith("requests[")) {
         log4j.debug("found element: requests");
         this.isRequestsUpdated = true;
         instantiateRequests(element);
         return;
      }
      // Leaving instantiateMessageSubTypeRefList()


      log4j.debug("Leaving " + METHOD);
   }

/*
 * This method instantiates the requests attribute which is a list of SubscriptionRequestSubType
 * @param Element element
 */
private void instantiateRequests(Element element) {
      // Entered instantiateSubTypeRefList()
   final String METHOD = "instantiateRequests()";
   log4j.debug("Entered " + METHOD);

   if (subscriptionRequests.getRequests() == null) {
   subscriptionRequests.setRequests(new ArrayList<SubscriptionRequestSubType>());
   }

   InstantiateSubscriptionRequestSubType instantiateSubscriptionRequestSubType = new InstantiateSubscriptionRequestSubType(element);
   SubscriptionRequestSubType subscriptionRequestSubType = instantiateSubscriptionRequestSubType.instantiate();
   subscriptionRequests.getRequests().add(subscriptionRequestSubType);
   log4j.debug("Leaving " + METHOD);
   // Leaving instantiateSubTypeRefList()
}

   /**
    * The validate method checks that a value was provided for all required 
    * attributes.
    *
    * @return The error message or null if no error occurred.
    */
   protected String validate() {
      final String METHOD = "validate()";
      log4j.debug("Entered " + METHOD);

      String result = null;

      // Entered buildValidationMethod()
      if (!this.isRequestsUpdated) {
         String msg = "Requests is required, but no value was provided.";
         if (result == null) {
            result = msg;
         }
      }

      if (!this.isSessionIdUpdated) {
         String msg = "SessionId is required, but no value was provided.";
         if (result == null) {
            result = msg;
         }
         else {
            result = result.concat("\n" + msg);
         }
      }


      if (result != null) {
         log4j.debug("result: " + result);
      }

      log4j.debug("Leaving " + METHOD);
      return result;
   }
}
