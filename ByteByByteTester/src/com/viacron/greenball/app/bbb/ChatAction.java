/*
 * WARNING: This file was generated by BitByBitGenerator.xtend (see
 * generateEnumFile()). 
 * Do not edit.
 */
package com.viacron.greenball.app.bbb;

import java.util.EnumSet;
import java.util.HashMap;
import java.util.Map;

public enum ChatAction {
    ADD(0, "Add"),
    DELETE(1, "Delete");

   private static final Map<Integer, ChatAction> lookup = new HashMap<Integer, ChatAction>();

   static {
      for (ChatAction enumType : EnumSet.allOf(ChatAction.class)) {
         lookup.put(enumType.getId(), enumType);
      }
   }

   private final int id;
   private final String value;

   ChatAction(int id, String v) {
      this.id = id;
      this.value = v;
   }

   public int getId() {
      return this.id;
   }

   public String value() {
      return this.value;
   }

   public static ChatAction fromValue(String v) {
      for (ChatAction chatAction : ChatAction.values()) {
         if (chatAction.value.equals(v)) {
               return chatAction;
         }
      }
      throw new IllegalArgumentException(v);
   }

   public static ChatAction toEnum(int id) {
      return lookup.get(id);
   }
}
