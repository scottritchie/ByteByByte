/*
 * WARNING: This file was generated by BitByBitGenerator.xtend (see
 * generateSubTypeInstantiationFile()).
 * Do not edit.
 */
package com.viacron.greenball.app.bbb;
import java.util.ArrayList;
import java.util.List;

import org.apache.log4j.Logger;

import com.viacron.utils.StringMessageBuffer.parser.AbstractAttribute;
import com.viacron.utils.StringMessageBuffer.parser.Element;
import com.viacron.utils.StringMessageBuffer.parser.KeyValuePair;

public class InstantiateSmsProviderSubType {
private static Logger log4j = Logger.getLogger(InstantiateSmsProviderSubType.class);

private Element element;
private SmsProviderSubType smsProviderSubType;

/*
 * The isUpdated flags. If an attribute is required by the grammar then a value
 * must be provided, and the isUpdated flag will be set when the value is
 * obtained during unpacking. After the object is constructed during unpacking, 
 * validation will be performed to make sure a value was provided for all attributes
 * that are not optional. 
 * See buildFlags() in JBitByBitGenerator.java.
 */
private boolean isCarrierUpdated = false;
private boolean isRegionUpdated = false;
private boolean isMailToSmsGatewayUpdated = false;

/**
 * This method constructs a InstantiateSmsProviderSubType object based on an Element
 * object.
 * 
 * @param element
 *            The Element.
 */
public InstantiateSmsProviderSubType(Element element) {
this.element = element;
this.smsProviderSubType = new SmsProviderSubType();
}

/**
 * The instantiate method instantiates a SmsProviderSubType object.
 * 
 * @return The SmsProviderSubType.
 */
protected SmsProviderSubType instantiate() {
final String METHOD = "instantiate()";
log4j.debug("Entered " + METHOD);

log4j.debug("element: " + element.getName());

for (AbstractAttribute abstractAttribute : element.getAbstractAttributes()) {
      // Entered instantiateSubTypeElements()
   // Leaving instantiateSubTypeElements()


   for (KeyValuePair kvp : abstractAttribute.getKeyValuePairs()) {
         // Entered instantiateSubTypeAttributes()
   if (kvp.getKey().equals("carrier")) {
    this.isCarrierUpdated = true;
   log4j.debug("setting value of \"carrier\" to: " + kvp.getValue());
   smsProviderSubType.setCarrier(kvp.getValue());
   }
   else if (kvp.getKey().equals("region")) {
    this.isRegionUpdated = true;
   log4j.debug("setting value of \"region\" to: " + kvp.getValue());
   smsProviderSubType.setRegion(kvp.getValue());
   }
   else if (kvp.getKey().equals("mailToSmsGateway")) {
    this.isMailToSmsGatewayUpdated = true;
   log4j.debug("setting value of \"mailToSmsGateway\" to: " + kvp.getValue());
   smsProviderSubType.setMailToSmsGateway(kvp.getValue());
   }
   // Leaving instantiateSubTypeAttributes()

   }
}

log4j.debug("Leaving " + METHOD);
return smsProviderSubType;
}

/**
 * The validate method checks that a value was provided for all required 
 * attributes.
 *
 * @return The error message or null if no error occurred.
 */
protected String validate() {
final String METHOD = "validate()";
log4j.debug("Entered " + METHOD);

String result = null;

// Entered buildValidationMethod()
if (!this.isCarrierUpdated) {
   String msg = "Carrier is required, but no value was provided.";
   if (result == null) {
      result = msg;
   }
}

if (!this.isRegionUpdated) {
   String msg = "Region is required, but no value was provided.";
   if (result == null) {
      result = msg;
   }
   else {
      result = result.concat("\n" + msg);
   }
}

if (!this.isMailToSmsGatewayUpdated) {
   String msg = "MailToSmsGateway is required, but no value was provided.";
   if (result == null) {
      result = msg;
   }
   else {
      result = result.concat("\n" + msg);
   }
}


if (result != null) {
   log4j.debug("result: " + result);
}

log4j.debug("Leaving " + METHOD);
return result;
}
}
