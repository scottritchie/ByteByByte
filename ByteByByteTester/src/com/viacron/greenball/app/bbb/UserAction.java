/*
 * WARNING: This file was generated by BitByBitGenerator.xtend (see
 * generateEnumFile()). 
 * Do not edit.
 */
package com.viacron.greenball.app.bbb;

import java.util.EnumSet;
import java.util.HashMap;
import java.util.Map;

public enum UserAction {
    SIGNUP(0, "SignUp"),
    LOGIN(1, "Login"),
    LOCKMETAMODEL(2, "LockMetaModel"),
    UNLOCKMETAMODEL(3, "UnlockMetaModel");

   private static final Map<Integer, UserAction> lookup = new HashMap<Integer, UserAction>();

   static {
      for (UserAction enumType : EnumSet.allOf(UserAction.class)) {
         lookup.put(enumType.getId(), enumType);
      }
   }

   private final int id;
   private final String value;

   UserAction(int id, String v) {
      this.id = id;
      this.value = v;
   }

   public int getId() {
      return this.id;
   }

   public String value() {
      return this.value;
   }

   public static UserAction fromValue(String v) {
      for (UserAction userAction : UserAction.values()) {
         if (userAction.value.equals(v)) {
               return userAction;
         }
      }
      throw new IllegalArgumentException(v);
   }

   public static UserAction toEnum(int id) {
      return lookup.get(id);
   }
}
