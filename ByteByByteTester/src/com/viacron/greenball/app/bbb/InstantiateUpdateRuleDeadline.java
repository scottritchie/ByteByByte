/*
 * WARNING: This file was generated by BitByBitGenerator.xtend (see
 * generateInstantiationFile()). 
 * Do not edit.
 */
package com.viacron.greenball.app.bbb;
import java.util.ArrayList;
import java.util.List;

import org.apache.log4j.Logger;

import com.viacron.utils.StringMessageBuffer.parser.AbstractAttribute;
import com.viacron.utils.StringMessageBuffer.parser.Element;
import com.viacron.utils.StringMessageBuffer.parser.KeyValuePair;

public class InstantiateUpdateRuleDeadline {
   private static Logger log4j = Logger.getLogger(InstantiateUpdateRuleDeadline.class);

   /*
    * The isUpdated flags. If an attribute is required by the grammar then a value
    * must be provided, and the isUpdated flag will be set when the value is
    * obtained during unpacking. After the object is constructed during unpacking, 
    * validation will be performed to make sure a value was provided for all attributes
    * that are not optional. 
    * See buildFlags() in JBitByBitGenerator.java.
    */
   private boolean isRuleIdUpdated = false;
   private boolean isNewDeadlineMsecUpdated = false;
   private boolean isOldDeadlineMsecUpdated = false;
   private boolean isSessionIdUpdated = false;

   private Element element;
   private UpdateRuleDeadline updateRuleDeadline;

   /**
    * The InstantiateUpdateRuleDeadline class instantiates the UpdateRuleDeadline object.
    *
    * @param element The root element returned by the parser after parsing the string
    *                representing the UpdateRuleDeadline.
    */
   public InstantiateUpdateRuleDeadline(Element element) {
      this.element = element;   
      this.updateRuleDeadline = new UpdateRuleDeadline();
   }

   /**
    * This method instantiates the UpdateRuleDeadline.
    *
    * @return The UpdateRuleDeadline.
    */
   public UpdateRuleDeadline instantiate() {
      final String METHOD = "instantiate()";
      log4j.debug("Entered " + METHOD);

      log4j.debug("UpdateRuleDeadline: " + element.getName());
      log4j.debug("# of attributes: " + element.getAbstractAttributes().size());

      instantiateUpdateRuleDeadlineAttributes(element);

      for (AbstractAttribute abstractAttribute : element
            .getAbstractAttributes()) {
         for (Element e : abstractAttribute.getElements()) {
            log4j.debug("element name: " + e.getName());
      instantiateUpdateRuleDeadlineSubTypeRefs(e);
      }
      }

      log4j.debug("Leaving " + METHOD);
      return updateRuleDeadline;
      }

      /*
       * This method instantiates the UpdateRuleDeadline attributes.
       * 
       * @param Element element
       */
      private void instantiateUpdateRuleDeadlineAttributes(Element element) {
      final String METHOD = "instantiateUpdateRuleDeadlineAttributes()";
      log4j.debug("Entered " + METHOD);

      log4j.debug("element name: " + element.getName());
      for (AbstractAttribute attr : element.getAbstractAttributes()) {
         for (KeyValuePair kvp : attr.getKeyValuePairs()) {
            log4j.debug("key: " + kvp.getKey());
            log4j.debug("value: " + kvp.getValue());
            // Entered instantiateMsgAttribute()
            if (kvp.getKey().equals("ruleId")) {
               log4j.debug("setting value of \"ruleId\" to: " + kvp.getValue());
               this.isRuleIdUpdated = true;
               updateRuleDeadline.setRuleId(kvp.getValue());
            }
            // Leaving instantiateMsgAttribute()
            // Entered instantiateMsgAttribute()
            if (kvp.getKey().equals("newDeadlineMsec")) {
               log4j.debug("setting value of \"newDeadlineMsec\" to: " + kvp.getValue());
               this.isNewDeadlineMsecUpdated = true;
               updateRuleDeadline.setNewDeadlineMsec(Long.parseLong(kvp.getValue()));
            }
            // Leaving instantiateMsgAttribute()
            // Entered instantiateMsgAttribute()
            if (kvp.getKey().equals("oldDeadlineMsec")) {
               log4j.debug("setting value of \"oldDeadlineMsec\" to: " + kvp.getValue());
               this.isOldDeadlineMsecUpdated = true;
               updateRuleDeadline.setOldDeadlineMsec(Long.parseLong(kvp.getValue()));
            }
            // Leaving instantiateMsgAttribute()
            // Entered instantiateMsgAttribute()
            if (kvp.getKey().equals("sessionId")) {
               log4j.debug("setting value of \"sessionId\" to: " + kvp.getValue());
               this.isSessionIdUpdated = true;
               updateRuleDeadline.setSessionId(kvp.getValue());
            }
            // Leaving instantiateMsgAttribute()
         }
      }

      log4j.debug("Leaving " + METHOD);
   }

   /*
    * This method instantiates the UpdateRuleDeadline SubType references.
    * 
    * @param Element element
    */
   private void instantiateUpdateRuleDeadlineSubTypeRefs(Element element) {
      final String METHOD = "instantiateUpdateRuleDeadlineSubTypeRefs()";
      log4j.debug("Entered " + METHOD);


      log4j.debug("Leaving " + METHOD);
   }


   /**
    * The validate method checks that a value was provided for all required 
    * attributes.
    *
    * @return The error message or null if no error occurred.
    */
   protected String validate() {
      final String METHOD = "validate()";
      log4j.debug("Entered " + METHOD);

      String result = null;

      // Entered buildValidationMethod()
      if (!this.isRuleIdUpdated) {
         String msg = "RuleId is required, but no value was provided.";
         if (result == null) {
            result = msg;
         }
      }

      if (!this.isNewDeadlineMsecUpdated) {
         String msg = "NewDeadlineMsec is required, but no value was provided.";
         if (result == null) {
            result = msg;
         }
         else {
            result = result.concat("\n" + msg);
         }
      }

      if (!this.isOldDeadlineMsecUpdated) {
         String msg = "OldDeadlineMsec is required, but no value was provided.";
         if (result == null) {
            result = msg;
         }
         else {
            result = result.concat("\n" + msg);
         }
      }

      if (!this.isSessionIdUpdated) {
         String msg = "SessionId is required, but no value was provided.";
         if (result == null) {
            result = msg;
         }
         else {
            result = result.concat("\n" + msg);
         }
      }


      if (result != null) {
         log4j.debug("result: " + result);
      }

      log4j.debug("Leaving " + METHOD);
      return result;
   }
}
