/*
 * WARNING: This file was generated by BitByBitGenerator.xtend (see
 * generateEnumFile()). 
 * Do not edit.
 */
package com.viacron.greenball.app.bbb;

import java.util.EnumSet;
import java.util.HashMap;
import java.util.Map;

public enum ModelElementAction {
    DELETE(0, "Delete"),
    UPDATE(1, "Update"),
    VALUE(2, "Value");

   private static final Map<Integer, ModelElementAction> lookup = new HashMap<Integer, ModelElementAction>();

   static {
      for (ModelElementAction enumType : EnumSet.allOf(ModelElementAction.class)) {
         lookup.put(enumType.getId(), enumType);
      }
   }

   private final int id;
   private final String value;

   ModelElementAction(int id, String v) {
      this.id = id;
      this.value = v;
   }

   public int getId() {
      return this.id;
   }

   public String value() {
      return this.value;
   }

   public static ModelElementAction fromValue(String v) {
      for (ModelElementAction modelElementAction : ModelElementAction.values()) {
         if (modelElementAction.value.equals(v)) {
               return modelElementAction;
         }
      }
      throw new IllegalArgumentException(v);
   }

   public static ModelElementAction toEnum(int id) {
      return lookup.get(id);
   }
}
