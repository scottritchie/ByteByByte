/*
 * WARNING: This file was generated by BitByBitGenerator.xtend (see
 * generateEnumFile()). 
 * Do not edit.
 */
package com.viacron.greenball.app.bbb;

import java.util.EnumSet;
import java.util.HashMap;
import java.util.Map;

public enum RuleState {
    SUBMITTED(0, "Submitted"),
    QUEUED(1, "Queued"),
    EXECUTING(2, "Executing"),
    EXPIRED(3, "Expired"),
    FAILED(4, "Failed");

   private static final Map<Integer, RuleState> lookup = new HashMap<Integer, RuleState>();

   static {
      for (RuleState enumType : EnumSet.allOf(RuleState.class)) {
         lookup.put(enumType.getId(), enumType);
      }
   }

   private final int id;
   private final String value;

   RuleState(int id, String v) {
      this.id = id;
      this.value = v;
   }

   public int getId() {
      return this.id;
   }

   public String value() {
      return this.value;
   }

   public static RuleState fromValue(String v) {
      for (RuleState ruleState : RuleState.values()) {
         if (ruleState.value.equals(v)) {
               return ruleState;
         }
      }
      throw new IllegalArgumentException(v);
   }

   public static RuleState toEnum(int id) {
      return lookup.get(id);
   }
}
