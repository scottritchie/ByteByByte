/*
 * WARNING: This file was generated by BitByBitGenerator.xtend (see
 * generateInstantiationFile()). 
 * Do not edit.
 */
package com.viacron.greenball.app.bbb;
import java.util.ArrayList;
import java.util.List;

import org.apache.log4j.Logger;

import com.viacron.utils.StringMessageBuffer.parser.AbstractAttribute;
import com.viacron.utils.StringMessageBuffer.parser.Element;
import com.viacron.utils.StringMessageBuffer.parser.KeyValuePair;

public class InstantiateMetaModelList1 {
   private static Logger log4j = Logger.getLogger(InstantiateMetaModelList1.class);

   /*
    * The isUpdated flags. If an attribute is required by the grammar then a value
    * must be provided, and the isUpdated flag will be set when the value is
    * obtained during unpacking. After the object is constructed during unpacking, 
    * validation will be performed to make sure a value was provided for all attributes
    * that are not optional. 
    * See buildFlags() in JBitByBitGenerator.java.
    */
   private boolean isMetaModelsUpdated = false;

   private Element element;
   private MetaModelList1 metaModelList1;

   /**
    * The InstantiateMetaModelList1 class instantiates the MetaModelList1 object.
    *
    * @param element The root element returned by the parser after parsing the string
    *                representing the MetaModelList1.
    */
   public InstantiateMetaModelList1(Element element) {
      this.element = element;   
      this.metaModelList1 = new MetaModelList1();
   }

   /**
    * This method instantiates the MetaModelList1.
    *
    * @return The MetaModelList1.
    */
   public MetaModelList1 instantiate() {
      final String METHOD = "instantiate()";
      log4j.debug("Entered " + METHOD);

      log4j.debug("MetaModelList1: " + element.getName());
      log4j.debug("# of attributes: " + element.getAbstractAttributes().size());

      instantiateMetaModelList1Attributes(element);

      for (AbstractAttribute abstractAttribute : element
            .getAbstractAttributes()) {
         for (Element e : abstractAttribute.getElements()) {
            log4j.debug("element name: " + e.getName());
      instantiateMetaModelList1SubTypeRefs(e);
      }
      }

      log4j.debug("Leaving " + METHOD);
      return metaModelList1;
      }

      /*
       * This method instantiates the MetaModelList1 attributes.
       * 
       * @param Element element
       */
      private void instantiateMetaModelList1Attributes(Element element) {
      final String METHOD = "instantiateMetaModelList1Attributes()";
      log4j.debug("Entered " + METHOD);

      log4j.debug("element name: " + element.getName());
      for (AbstractAttribute attr : element.getAbstractAttributes()) {
         for (KeyValuePair kvp : attr.getKeyValuePairs()) {
            log4j.debug("key: " + kvp.getKey());
            log4j.debug("value: " + kvp.getValue());
         }
      }

      log4j.debug("Leaving " + METHOD);
   }

   /*
    * This method instantiates the MetaModelList1 SubType references.
    * 
    * @param Element element
    */
   private void instantiateMetaModelList1SubTypeRefs(Element element) {
      final String METHOD = "instantiateMetaModelList1SubTypeRefs()";
      log4j.debug("Entered " + METHOD);

      // Entered instantiateMessageSubTypeRefList()
      if (element.getName().startsWith("metaModels[")) {
         log4j.debug("found element: metaModels");
         this.isMetaModelsUpdated = true;
         instantiateMetaModels(element);
         return;
      }
      // Leaving instantiateMessageSubTypeRefList()


      log4j.debug("Leaving " + METHOD);
   }

/*
 * This method instantiates the metaModels attribute which is a list of MetaModel1
 * @param Element element
 */
private void instantiateMetaModels(Element element) {
      // Entered instantiateSubTypeRefList()
   final String METHOD = "instantiateMetaModels()";
   log4j.debug("Entered " + METHOD);

   if (metaModelList1.getMetaModels() == null) {
   metaModelList1.setMetaModels(new ArrayList<MetaModel1>());
   }

   InstantiateMetaModel1 instantiateMetaModel1 = new InstantiateMetaModel1(element);
   MetaModel1 metaModel1 = instantiateMetaModel1.instantiate();
   metaModelList1.getMetaModels().add(metaModel1);
   log4j.debug("Leaving " + METHOD);
   // Leaving instantiateSubTypeRefList()
}

   /**
    * The validate method checks that a value was provided for all required 
    * attributes.
    *
    * @return The error message or null if no error occurred.
    */
   protected String validate() {
      final String METHOD = "validate()";
      log4j.debug("Entered " + METHOD);

      String result = null;

      // Entered buildValidationMethod()
      if (!this.isMetaModelsUpdated) {
         String msg = "MetaModels is required, but no value was provided.";
         if (result == null) {
            result = msg;
         }
      }


      if (result != null) {
         log4j.debug("result: " + result);
      }

      log4j.debug("Leaving " + METHOD);
      return result;
   }
}
