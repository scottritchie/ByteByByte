/*
 * WARNING: This file was generated by BitByBitGenerator.xtend (see
 * generateEnumFile()). 
 * Do not edit.
 */
package com.viacron.greenball.app.bbb;

import java.util.EnumSet;
import java.util.HashMap;
import java.util.Map;

public enum RulePart {
    DEADLINE(0, "Deadline"),
    SCOPE(1, "Scope"),
    TEST(2, "Test");

   private static final Map<Integer, RulePart> lookup = new HashMap<Integer, RulePart>();

   static {
      for (RulePart enumType : EnumSet.allOf(RulePart.class)) {
         lookup.put(enumType.getId(), enumType);
      }
   }

   private final int id;
   private final String value;

   RulePart(int id, String v) {
      this.id = id;
      this.value = v;
   }

   public int getId() {
      return this.id;
   }

   public String value() {
      return this.value;
   }

   public static RulePart fromValue(String v) {
      for (RulePart rulePart : RulePart.values()) {
         if (rulePart.value.equals(v)) {
               return rulePart;
         }
      }
      throw new IllegalArgumentException(v);
   }

   public static RulePart toEnum(int id) {
      return lookup.get(id);
   }
}
