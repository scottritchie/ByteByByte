/*
 * WARNING: This file was generated by BitByBitGenerator.xtend (see
 * generateClassFile()). 
 * Do not edit.
 */
package com.viacron.greenball.app.bbb;

import java.nio.charset.Charset;
import java.util.ArrayList;
import java.util.EnumSet;
import java.util.HashMap;
import java.util.List;
import java.util.Map;

import org.apache.log4j.Logger;
import org.antlr.runtime.ANTLRStringStream;
import org.antlr.runtime.CommonTokenStream;
import org.antlr.runtime.RecognitionException;

import com.viacron.utils.StringMessageBuffer.parser.AbstractAttribute;
import com.viacron.utils.StringMessageBuffer.parser.Element;
import com.viacron.utils.StringMessageBuffer.parser.KeyValuePair;
import com.viacron.utils.StringMessageBuffer.parser.SMBGLexer;
import com.viacron.utils.StringMessageBuffer.parser.SMBGParser;

/**
 * The ResponseLoadModel class defines a message of the messaging system defined
 * by the GreenballMessages grammar. It provides methods for packing a ResponseLoadModel object
 * into a byte array or string, and for unpacking a byte array or string
 * back into a ResponseLoadModel object.
 */
public class ResponseLoadModel {
   private static final int ID = GreenballMessagesEnum.RESPONSE_LOAD_MODEL.getId();

   /**
    * The number of bits required to represent the ResponseLoadModelEnum IDs.
    * # of attributes: 7
    * # of PEnumRefs: 0
    */
   private static final int ID_BITS = 3;

   private static GreenballMessagesDirectoryEntry[] theDirectory;
   private static final int DIRECTORY_OFFSET = 2;

   private static Logger log4j = Logger.getLogger(ResponseLoadModel.class);

   // Entered buildClassEnum()
   private enum ResponseLoadModelEnum {
      // This is a directory item. It's offset and length cannot be determined
      // until runtime.
      ORG_ID(0, -1, 0, true),

      // This is a directory item. It's offset and length cannot be determined
      // until runtime.
      ORG_NAME(1, -1, 0, true),

      // This is a directory item. It's offset and length cannot be determined
      // until runtime.
      NAME(2, -1, 0, true),

      // This is a directory item. It's offset and length cannot be determined
      // until runtime.
      RULES(3, -1, 0, true),

      // This is a directory item. It's offset and length cannot be determined
      // until runtime.
      TREE_NODE(4, -1, 0, true),

      // This is a directory item. It's offset and length cannot be determined
      // until runtime.
      ID(5, -1, 0, true),

      // This is a directory item. It's offset and length cannot be determined
      // until runtime.
      SESSION_ID(6, -1, 0, true);

      private final int id;
      private final int offsetInBits;
      private int length;
      private boolean isDirectoryEntry;

      ResponseLoadModelEnum(int id, int offsetInBits, int length, boolean isDirectoryEntry) {
         this.id = id;
         this.offsetInBits = offsetInBits;
         this.length = length;
         this.isDirectoryEntry = isDirectoryEntry;
      }

      public int getId() {
         return this.id;
      }

      public int getOffsetInBits() {
         return this.offsetInBits;
      }

      public int getLength() {
         return this.length;
      }

      public void setLength(int length) {
         this.length = length;
      }
   } // Leaving buildClassEnum()

   /*
    * The instance variables.
    * See buildInstanceVariables() in JBitByBitGenerator.java.
    */
   private String orgId;
   private String orgName;
   private String name;
   private List<Rule> rules;
   private TreeNodeType treeNode;
   private String id;
   private String sessionId;

   private String theErrorMsg;

   /**
    * The ResponseLoadModel no-arg constructor.
    */
   public ResponseLoadModel() {}

   /**
    * The ResponseLoadModel constructor.
    * See buildParameterAnnotations() in JBitByBitGenerator.java.
    *
    * @param orgId The orgId message attribute.
    * @param orgName The orgName message attribute.
    * @param name The name message attribute.
    * @param rules The rules message attribute.
    * @param treeNode The treeNode message attribute.
    * @param id The id message attribute.
    * @param sessionId The sessionId message attribute.
    */
   public ResponseLoadModel(String orgId,
                            String orgName,
                            String name, List<Rule> rules, TreeNodeType treeNode,
                            String id,
                            String sessionId) {
      // Entered buildParameterAssignments()
      this.setOrgId(orgId);
      this.setOrgName(orgName);
      this.setName(name);
      this.setRules(rules);
      this.setTreeNode(treeNode);
      this.setId(id);
      this.setSessionId(sessionId);
      // Leaving buildParameterAssignments()
   }


   // Entered buildSetErrorMsgMethod()
   private void setErrorMsg(String errorMsg) {
      theErrorMsg = errorMsg;
   }
   // Leaving buildSetErrorMsgMethod()

   // Entered buildGetErrorMsgMethod()
   /**
    * This method returns the error message or null if no error
    * occurred.
    */
   public String getErrorMsg() {
      return theErrorMsg;
   }
   // Leaving buildGetErrorMsgMethod()

   // Entered buildPackMethod()
   /**
    * This method packs a ResponseLoadModel object into a byte array. If a required attribute is not
    * provided an exception will be thrown.
    *
    * @return A byte array which represents a ResponseLoadModel message object.
    * @throws MissingAttributeException
    */
   public byte[] pack() throws MissingAttributeException {
      final String METHOD = "pack()";
      log4j.debug("Entered " + METHOD);

      int byteArraySize = getResponseLoadModelSize();
      log4j.debug("byteArraySize: " + byteArraySize);
      byte[] bb = new byte[byteArraySize];

      /*
       * Convert object to byte array.
       */
      int offset = 0;

      bb = GreenballMessagesUtility.insertShort(bb, offset, (short) ID);
      offset += 2;


      ResponseLoadModelEnum.ORG_ID.setLength(this.getOrgId().length());
      ResponseLoadModelEnum.ORG_NAME.setLength(this.getOrgName().length());
      ResponseLoadModelEnum.NAME.setLength(this.getName().length());
      ResponseLoadModelEnum.RULES.setLength(this.getRules().size());
      ResponseLoadModelEnum.TREE_NODE.setLength(this.getTreeNode().getTreeNodeTypeSize());
      ResponseLoadModelEnum.ID.setLength(this.getId().length());
      ResponseLoadModelEnum.SESSION_ID.setLength(this.getSessionId().length());

      int directorySize = getResponseLoadModelDirectorySize();
      if (directorySize != 0) {
         // Create the directory
         theDirectory = new GreenballMessagesDirectoryEntry[directorySize];
         for (int i = 0; i < directorySize; i++) {
            theDirectory[i] = new GreenballMessagesDirectoryEntry();
         }

         // Fill the directory
         int index = 0;
         offset = DIRECTORY_OFFSET
               + GreenballMessagesDirectoryEntry.DIRECTORY_ENTRY_SIZE_IN_BYTES
               * theDirectory.length;

         for (ResponseLoadModelEnum responseLoadModelEnum : ResponseLoadModelEnum.values()) {
            if (responseLoadModelEnum.isDirectoryEntry == true) {
               int id = responseLoadModelEnum.getId();
               int length = responseLoadModelEnum.getLength();

               theDirectory[index].setId(id);
               theDirectory[index].setLength(length);
               theDirectory[index].setOffset(offset);

               // Insert the data
               if (id == ResponseLoadModelEnum.ORG_ID.getId()) {
                  log4j.debug(String.format("inserting %s at offset %d", "orgId", offset));
                  bb = GreenballMessagesUtility.insertString(bb, offset, getOrgId());
                  offset += length * 1;
               }
               else if (id == ResponseLoadModelEnum.ORG_NAME.getId()) {
                  log4j.debug(String.format("inserting %s at offset %d", "orgName", offset));
                  bb = GreenballMessagesUtility.insertString(bb, offset, getOrgName());
                  offset += length * 1;
               }
               else if (id == ResponseLoadModelEnum.NAME.getId()) {
                  log4j.debug(String.format("inserting %s at offset %d", "name", offset));
                  bb = GreenballMessagesUtility.insertString(bb, offset, getName());
                  offset += length * 1;
               }
               else if (id == ResponseLoadModelEnum.RULES.getId()) {
                  log4j.debug(String.format("inserting %s at offset %d", "rules", offset));
                  for (Rule rule : this.rules) {
                     byte[] bytes = rule.pack();
                     bb = GreenballMessagesUtility.insertBytes(bb, offset, bytes);

                     theDirectory[index].setId(id);
                     theDirectory[index].setLength(bytes.length);
                     theDirectory[index].setOffset(offset);

                     offset += bytes.length;
                     index++;
                  }
               }
               else if (id == ResponseLoadModelEnum.TREE_NODE.getId()) {
                  log4j.debug(String.format("inserting %s at offset %d", "treeNode", offset));
                  byte[] bytes = this.getTreeNode().pack();
                  bb = GreenballMessagesUtility.insertBytes(bb, offset, bytes);
                  offset += bytes.length;
               }
               else if (id == ResponseLoadModelEnum.ID.getId()) {
                  log4j.debug(String.format("inserting %s at offset %d", "id", offset));
                  bb = GreenballMessagesUtility.insertString(bb, offset, getId());
                  offset += length * 1;
               }
               else if (id == ResponseLoadModelEnum.SESSION_ID.getId()) {
                  log4j.debug(String.format("inserting %s at offset %d", "sessionId", offset));
                  bb = GreenballMessagesUtility.insertString(bb, offset, getSessionId());
                  offset += length * 1;
               }
               index++;
            }
         }

         // Insert the directory
         offset = DIRECTORY_OFFSET;
         for (GreenballMessagesDirectoryEntry directoryEntry : theDirectory) {
            bb = GreenballMessagesUtility.insertInteger(bb, offset, directoryEntry.getId());
            bb = GreenballMessagesUtility.insertInteger(bb, offset + 4, directoryEntry.getLength());
            bb = GreenballMessagesUtility.insertInteger(bb, offset + 8, directoryEntry.getOffset());

            offset += GreenballMessagesDirectoryEntry.DIRECTORY_ENTRY_SIZE_IN_BYTES;
            index++;
         }

         // Calculate and insert the checksum
         long checksum = GreenballMessagesUtility.getChecksum(bb, bb.length - 8);
         bb = GreenballMessagesUtility.insertLong(bb, bb.length - 8, checksum);
      }
      log4j.debug("Leaving " + METHOD);
      return bb;
   }
   // Leaving buildPackMethod()

   // Entered buildUnpackMethod()
   /**
    * This method unpacks a byte array containing a ResponseLoadModel object.
    *
    * @return The byte array as a ResponseLoadModel message object.
    */
   public ResponseLoadModel unpack(byte[] ba) {
      final String METHOD = "unpack()";
      log4j.debug("Entered " + METHOD);

      ResponseLoadModel responseLoadModel = new ResponseLoadModel();


      // Get the directory
      int directorySize = getResponseLoadModelDirectorySize();
      theDirectory = new GreenballMessagesDirectoryEntry[directorySize];
      int dirOffset = DIRECTORY_OFFSET;

      for (int entry = 0 ; entry < directorySize; entry++) {
         GreenballMessagesDirectoryEntry directoryEntry = new GreenballMessagesDirectoryEntry();

         int id = GreenballMessagesUtility.getInteger(ba, dirOffset * 8);
         directoryEntry.setId(id);
         int length = GreenballMessagesUtility.getInteger(ba, (dirOffset + 4) * 8);
         directoryEntry.setLength(length);
         int offset = GreenballMessagesUtility.getInteger(ba, (dirOffset + 8) * 8);
         directoryEntry.setOffset(offset);

         theDirectory[entry] = directoryEntry;

         dirOffset += GreenballMessagesDirectoryEntry.DIRECTORY_ENTRY_SIZE_IN_BYTES;
      }

      for (GreenballMessagesDirectoryEntry directoryEntry : theDirectory) {
         int id = directoryEntry.getId();
         int offset = directoryEntry.getOffset();
         int length = directoryEntry.getLength();

         if (id == ResponseLoadModelEnum.ORG_ID.getId()) {
            responseLoadModel.setOrgId(GreenballMessagesUtility.getString(ba, offset, length));
         }
         else if (id == ResponseLoadModelEnum.ORG_NAME.getId()) {
            responseLoadModel.setOrgName(GreenballMessagesUtility.getString(ba, offset, length));
         }
         else if (id == ResponseLoadModelEnum.NAME.getId()) {
            responseLoadModel.setName(GreenballMessagesUtility.getString(ba, offset, length));
         }
         else if (id == ResponseLoadModelEnum.ID.getId()) {
            responseLoadModel.setId(GreenballMessagesUtility.getString(ba, offset, length));
         }
         else if (id == ResponseLoadModelEnum.SESSION_ID.getId()) {
            responseLoadModel.setSessionId(GreenballMessagesUtility.getString(ba, offset, length));
         }
         else if (id == ResponseLoadModelEnum.RULES.getId()) {
            byte[] bytes = new byte[length];
            for (int index = 0; index < length; index++) {
               bytes[index] = GreenballMessagesUtility.getByte(ba, (offset + index) * 8);
            }
            responseLoadModel.getRules().add(new Rule().unpack(bytes));
         }
         else if (id == ResponseLoadModelEnum.TREE_NODE.getId()) {
            byte[] bytes = new byte[length];

            for (int index = 0; index < length; index++) {
               bytes[index] = GreenballMessagesUtility.getByte(ba, (offset + index) * 8);
            }

            responseLoadModel.setTreeNode(new TreeNodeType().unpack(bytes));
         }
      }

      log4j.debug("Leaving " + METHOD);
      return responseLoadModel;
   }
   // Leaving buildUnpackMethod()

   // Entered buildPackStrMethod()
   /**
    * This method packs a ResponseLoadModel object into a String. If a required attribute is not
    * provided an exception will be thrown.
    *
    * @return A String which represents a ResponseLoadModel message object.
    * @throws MissingAttributeException
    */
   public String packStr() throws MissingAttributeException {
      final String METHOD = "packStr()";
      log4j.debug("Entered " + METHOD);

      String msg = null;
      setErrorMsg(null);
       
      /*
       * Initialize attribute values.
       */
      InitializeResponseLoadModel initializeResponseLoadModel = new InitializeResponseLoadModel(this);
      Element element = initializeResponseLoadModel.initialize();

      if (getErrorMsg() == null) {
         /*
          * Convert object to String.
          * See convertObjectToString() in JBitByBitGenerator.java.
          */
         msg = "{" + element.getName();
         for (AbstractAttribute attr : element.getAbstractAttributes()) {
            msg = buildString(attr, msg);
         }
         msg = msg.concat("}");

         log4j.debug("msg: " + msg);
      }

      log4j.debug("Leaving " + METHOD);
      return msg;
   }
   // Leaving buildPackStrMethod()

   // Entered buildUnpackStrMethod()
   /**
    * The unpack() method unpacks a String into an object. The String must conform to the
    * SMBG grammar.
    *
     * @return The message string as an object.
    */
   public ResponseLoadModel unpack(String buffer) {
      final String METHOD = "unpack()";
      log4j.debug("Entered " + METHOD);

      setErrorMsg(null);

      // Create the lexer
      log4j.debug("lexing...");
      SMBGLexer lexer = new SMBGLexer(new ANTLRStringStream(buffer));

      int nbrOfSyntaxErrors = lexer.getNumberOfSyntaxErrors();
      log4j.debug("# of lexer errors: " + nbrOfSyntaxErrors);

      // Check for errors
      if (nbrOfSyntaxErrors != 0) {
         for (String error : lexer.getErrors()) {
            log4j.error(error);
         }

         return null;
      }

      // Wrap a token-stream around the lexer
      CommonTokenStream tokenStream = new CommonTokenStream(lexer);

      // Create the parser
      log4j.debug("parsing...");
      SMBGParser parser = new SMBGParser(tokenStream);

      // Check for errors
      nbrOfSyntaxErrors = parser.getNumberOfSyntaxErrors();
      log4j.debug("# of parse errors: " + nbrOfSyntaxErrors);

      if (nbrOfSyntaxErrors != 0) {
         for (String error : parser.getErrors()) {
               log4j.error(error);
         }

         return null;
      }

      // Unpack the string into objects
      log4j.debug("unpacking...");
      try {
         parser.unpack();
      } catch (RecognitionException e) {
         log4j.error("Error: " + e.getMessage());
         setErrorMsg(e.getMessage());
         return null;
      }
      
      Element element = parser.getElement();
      element.dump();

      /*
       * Instantiate the ResponseLoadModel object.
       */
       InstantiateResponseLoadModel instantiateResponseLoadModel = new InstantiateResponseLoadModel(element);
       ResponseLoadModel responseLoadModel = instantiateResponseLoadModel.instantiate();

      /*
       * Verify that a value was provided for all mandatory attributes.
       */
      String result = instantiateResponseLoadModel.validate();
      if (result != null) {
         log4j.error("Error: " + result);
         setErrorMsg(result);
         return null;
      }

      log4j.debug("Leaving " + METHOD);
      return responseLoadModel;
   }
   // Leaving buildUnpackStrMethod()

   private String buildString(AbstractAttribute attr, String msg) {
      for (KeyValuePair kvp : attr.getKeyValuePairs()) {
         msg = msg.concat(" " + kvp.getKey() + ":" + kvp.getValue());
      }

      for (Element element : attr.getElements()) {
         msg = msg.concat(" {" + element.getName());
         for (AbstractAttribute abstractAttribute : element.getAbstractAttributes()) {
            msg = buildString(abstractAttribute, msg);
         }
         msg = msg.concat("}");
      }

      return msg;
   }

   /**
    * This method sets the orgId attribute. Setting of this value is required.
    * @param orgId The orgId attribute.
    */
   public void setOrgId(String orgId) {
      this.orgId = orgId;
   }

   /**
    * This method sets the orgName attribute. Setting of this value is required.
    * @param orgName The orgName attribute.
    */
   public void setOrgName(String orgName) {
      this.orgName = orgName;
   }

   /**
    * This method sets the name attribute. Setting of this value is optional.
    * @param name The name attribute.
    */
   public void setName(String name) {
      this.name = name;
   }

   /**
    * This method sets the id attribute. Setting of this value is required.
    * @param id The id attribute.
    */
   public void setId(String id) {
      this.id = id;
   }

   /**
    * This method sets the sessionId attribute. Setting of this value is optional.
    * @param sessionId The sessionId attribute.
    */
   public void setSessionId(String sessionId) {
      this.sessionId = sessionId;
   }

   /**
    * This method gets the orgId attribute. The orgId attribute is a required attribute.
    */
   public String getOrgId() {
      return this.orgId;
   }

   /**
    * This method gets the orgName attribute. The orgName attribute is a required attribute.
    */
   public String getOrgName() {
      return this.orgName;
   }

   /**
    * This method gets the name attribute. The name attribute is an optional attribute.
    */
   public String getName() {
      return this.name;
   }

   /**
    * This method gets the id attribute. The id attribute is a required attribute.
    */
   public String getId() {
      return this.id;
   }

   /**
    * This method gets the sessionId attribute. The sessionId attribute is an optional attribute.
    */
   public String getSessionId() {
      return this.sessionId;
   }

   /**
    * This method sets the rules attribute. Setting of this value is optional.
    * @param rules The rules.
    */
   public void setRules(List<Rule> rules) {
      this.rules = rules;
   }

   /**
    * This method sets the treeNode attribute. Setting of this value is required.
    * @param treeNode The treeNode.
    */
   public void setTreeNode(TreeNodeType treeNode) {
      this.treeNode = treeNode;
   }

   /**
    * This method gets the rules attribute. The rules attribute is an optional attribute.
    */
   public List<Rule> getRules() {
      if (this.rules == null) {
         this.rules = new ArrayList<Rule>();
      }
      return this.rules;
   }

   /**
    * This method gets the treeNode attribute. The treeNode attribute is a required attribute.
    */
   public TreeNodeType getTreeNode() {
      return this.treeNode;
   }

   /**
    * This method gets the size of the directory.
    */
   private int getResponseLoadModelDirectorySize() {
      int directorySize = 0;

      // orgId
      directorySize++;
      // orgName
      directorySize++;
      // name
      directorySize++;
      // rules
      directorySize += getRules().size();
      // treeNode
      directorySize++;
      // id
      directorySize++;
      // sessionId
      directorySize++;

      return directorySize;
   }


   /**
    * This method gets the size of the ResponseLoadModel object.
    * @return The size in bytes.
    */
   public int getResponseLoadModelSize() {
      int size = 0;

      int directorySize = getResponseLoadModelDirectorySize();
      size += directorySize * GreenballMessagesDirectoryEntry.DIRECTORY_ENTRY_SIZE_IN_BYTES;
      // Add two bytes for the ID
      size += 2;
      // Add eight bytes for the CRC
      size += 8;
      // Add size of orgId
      size += (getOrgId() != null ? getOrgId().length() : 0);
      // Add size of orgName
      size += (getOrgName() != null ? getOrgName().length() : 0);
      // Add size of name
      size += (getName() != null ? getName().length() : 0);
      // Add size of id
      size += (getId() != null ? getId().length() : 0);
      // Add size of sessionId
      size += (getSessionId() != null ? getSessionId().length() : 0);
      // Add size of rules
      for (Rule rule: getRules()) {
         size += rule.getRuleSize();
      }
      // Add size of treeNode
      size += getTreeNode().getTreeNodeTypeSize();

      return size;
   }

   @Override
   public String toString() {
      StringBuilder result = new StringBuilder();
      String NEW_LINE = System.getProperty("line.separator");

      result.append(this.getClass().getSimpleName() + " Object {" + NEW_LINE);
      result.append(" orgId: " + orgId + NEW_LINE);
      result.append(" orgName: " + orgName + NEW_LINE);
      result.append(" name: " + name + NEW_LINE);

      if (rules != null) {
         for (Rule rulesRule : rules) {
            result.append(rulesRule.toString());
         }
      }
      result.append(" treeNode: " + treeNode + NEW_LINE);
      result.append(" id: " + id + NEW_LINE);
      result.append(" sessionId: " + sessionId + NEW_LINE);

      result.append("} ");

      return result.toString();
   }

   /**
    * The hashCode method returns this object's hashCode.
    */
   @Override
   public int hashCode() {
      final int prime = 31;
      int result = 1;
      result = prime * result
         + ((String) orgId).hashCode();
      result = prime * result
         + ((String) orgName).hashCode();
      result = prime * result
         + ((String) name).hashCode();
      result = prime * result
         + ((String) id).hashCode();
      result = prime * result
         + ((String) sessionId).hashCode();
      return result;
   }

   /**
    * The equals method checks for equality. 
    */
   public boolean equals(Object obj) {
      if (obj == this) return true;
      if (obj == null) return false;
      if (getClass() != obj.getClass()) return false;

      ResponseLoadModel responseLoadModel = (ResponseLoadModel) obj;
      if (!responseLoadModel.getOrgId().equals(this.getOrgId())) {
         return false;
      }

      if (!responseLoadModel.getOrgName().equals(this.getOrgName())) {
         return false;
      }

      if (responseLoadModel.getName() == null && this.getName() != null) {
         return false;
      }
      else if (responseLoadModel.getName() != null && this.getName() == null) {
         return false;
      }
      else if (responseLoadModel.getName() != null && 
      !responseLoadModel.getName().equals(this.getName())) {
         return false;
      }

      if (!responseLoadModel.getId().equals(this.getId())) {
         return false;
      }

      if (responseLoadModel.getSessionId() == null && this.getSessionId() != null) {
         return false;
      }
      else if (responseLoadModel.getSessionId() != null && this.getSessionId() == null) {
         return false;
      }
      else if (responseLoadModel.getSessionId() != null && 
      !responseLoadModel.getSessionId().equals(this.getSessionId())) {
         return false;
      }

      return true;
   }
}
