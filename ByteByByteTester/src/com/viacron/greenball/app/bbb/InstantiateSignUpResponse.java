/*
 * WARNING: This file was generated by BitByBitGenerator.xtend (see
 * generateInstantiationFile()). 
 * Do not edit.
 */
package com.viacron.greenball.app.bbb;
import java.util.ArrayList;
import java.util.List;

import org.apache.log4j.Logger;

import com.viacron.utils.StringMessageBuffer.parser.AbstractAttribute;
import com.viacron.utils.StringMessageBuffer.parser.Element;
import com.viacron.utils.StringMessageBuffer.parser.KeyValuePair;

public class InstantiateSignUpResponse {
   private static Logger log4j = Logger.getLogger(InstantiateSignUpResponse.class);

   /*
    * The isUpdated flags. If an attribute is required by the grammar then a value
    * must be provided, and the isUpdated flag will be set when the value is
    * obtained during unpacking. After the object is constructed during unpacking, 
    * validation will be performed to make sure a value was provided for all attributes
    * that are not optional. 
    * See buildFlags() in JBitByBitGenerator.java.
    */
   private boolean isEmailAddressUpdated = false;
   private boolean isPasswordUpdated = false;
   private boolean isAlreadyInUseUpdated = false;

   private Element element;
   private SignUpResponse signUpResponse;

   /**
    * The InstantiateSignUpResponse class instantiates the SignUpResponse object.
    *
    * @param element The root element returned by the parser after parsing the string
    *                representing the SignUpResponse.
    */
   public InstantiateSignUpResponse(Element element) {
      this.element = element;   
      this.signUpResponse = new SignUpResponse();
   }

   /**
    * This method instantiates the SignUpResponse.
    *
    * @return The SignUpResponse.
    */
   public SignUpResponse instantiate() {
      final String METHOD = "instantiate()";
      log4j.debug("Entered " + METHOD);

      log4j.debug("SignUpResponse: " + element.getName());
      log4j.debug("# of attributes: " + element.getAbstractAttributes().size());

      instantiateSignUpResponseAttributes(element);

      for (AbstractAttribute abstractAttribute : element
            .getAbstractAttributes()) {
         for (Element e : abstractAttribute.getElements()) {
            log4j.debug("element name: " + e.getName());
      instantiateSignUpResponseSubTypeRefs(e);
      }
      }

      log4j.debug("Leaving " + METHOD);
      return signUpResponse;
      }

      /*
       * This method instantiates the SignUpResponse attributes.
       * 
       * @param Element element
       */
      private void instantiateSignUpResponseAttributes(Element element) {
      final String METHOD = "instantiateSignUpResponseAttributes()";
      log4j.debug("Entered " + METHOD);

      log4j.debug("element name: " + element.getName());
      for (AbstractAttribute attr : element.getAbstractAttributes()) {
         for (KeyValuePair kvp : attr.getKeyValuePairs()) {
            log4j.debug("key: " + kvp.getKey());
            log4j.debug("value: " + kvp.getValue());
            // Entered instantiateMsgAttribute()
            if (kvp.getKey().equals("emailAddress")) {
               log4j.debug("setting value of \"emailAddress\" to: " + kvp.getValue());
               this.isEmailAddressUpdated = true;
               signUpResponse.setEmailAddress(kvp.getValue());
            }
            // Leaving instantiateMsgAttribute()
            // Entered instantiateMsgAttribute()
            if (kvp.getKey().equals("password")) {
               log4j.debug("setting value of \"password\" to: " + kvp.getValue());
               this.isPasswordUpdated = true;
               signUpResponse.setPassword(kvp.getValue());
            }
            // Leaving instantiateMsgAttribute()
            // Entered instantiateMsgAttribute()
            if (kvp.getKey().equals("alreadyInUse")) {
               log4j.debug("setting value of \"alreadyInUse\" to: " + kvp.getValue());
               this.isAlreadyInUseUpdated = true;
               signUpResponse.setAlreadyInUse(kvp.getValue().equals("true"));
            }
            // Leaving instantiateMsgAttribute()
            // Entered instantiateMsgAttribute()
            if (kvp.getKey().equals("sessionId")) {
               log4j.debug("setting value of \"sessionId\" to: " + kvp.getValue());
               signUpResponse.setSessionId(kvp.getValue());
            }
            // Leaving instantiateMsgAttribute()
         }
      }

      log4j.debug("Leaving " + METHOD);
   }

   /*
    * This method instantiates the SignUpResponse SubType references.
    * 
    * @param Element element
    */
   private void instantiateSignUpResponseSubTypeRefs(Element element) {
      final String METHOD = "instantiateSignUpResponseSubTypeRefs()";
      log4j.debug("Entered " + METHOD);


      log4j.debug("Leaving " + METHOD);
   }


   /**
    * The validate method checks that a value was provided for all required 
    * attributes.
    *
    * @return The error message or null if no error occurred.
    */
   protected String validate() {
      final String METHOD = "validate()";
      log4j.debug("Entered " + METHOD);

      String result = null;

      // Entered buildValidationMethod()
      if (!this.isEmailAddressUpdated) {
         String msg = "EmailAddress is required, but no value was provided.";
         if (result == null) {
            result = msg;
         }
      }

      if (!this.isPasswordUpdated) {
         String msg = "Password is required, but no value was provided.";
         if (result == null) {
            result = msg;
         }
         else {
            result = result.concat("\n" + msg);
         }
      }

      if (!this.isAlreadyInUseUpdated) {
         String msg = "AlreadyInUse is required, but no value was provided.";
         if (result == null) {
            result = msg;
         }
         else {
            result = result.concat("\n" + msg);
         }
      }


      if (result != null) {
         log4j.debug("result: " + result);
      }

      log4j.debug("Leaving " + METHOD);
      return result;
   }
}
