/*
 * WARNING: This file was generated by BitByBitGenerator.xtend (see
 * generateEnumFile()). 
 * Do not edit.
 */
package com.viacron.greenball.app.bbb;

import java.util.EnumSet;
import java.util.HashMap;
import java.util.Map;

public enum ModelAction {
    ADD(0, "Add"),
    DELETE(1, "Delete"),
    MODIFY(2, "Modify"),
    SHOW(3, "Show");

   private static final Map<Integer, ModelAction> lookup = new HashMap<Integer, ModelAction>();

   static {
      for (ModelAction enumType : EnumSet.allOf(ModelAction.class)) {
         lookup.put(enumType.getId(), enumType);
      }
   }

   private final int id;
   private final String value;

   ModelAction(int id, String v) {
      this.id = id;
      this.value = v;
   }

   public int getId() {
      return this.id;
   }

   public String value() {
      return this.value;
   }

   public static ModelAction fromValue(String v) {
      for (ModelAction modelAction : ModelAction.values()) {
         if (modelAction.value.equals(v)) {
               return modelAction;
         }
      }
      throw new IllegalArgumentException(v);
   }

   public static ModelAction toEnum(int id) {
      return lookup.get(id);
   }
}
