/*
 * WARNING: This file was generated by BitByBitGenerator.xtend (see
 * generateClassFile()). 
 * Do not edit.
 */
package com.viacron.greenball.app.bbb;

import java.nio.charset.Charset;
import java.util.ArrayList;
import java.util.EnumSet;
import java.util.HashMap;
import java.util.List;
import java.util.Map;

import org.apache.log4j.Logger;
/**
 * The CrossReference class is a SubType referenced by message objects defined
 * by the GreenballMessages grammar.
 */
public class CrossReference {
   private static final int ID = GreenballMessagesEnum.CROSS_REFERENCE.getId();

   /**
    * The number of bits required to represent the CrossReferenceEnum IDs.
    * # of attributes: 3
    * # of PEnumRefs: 0
    */
   private static final int ID_BITS = 2;

   private static GreenballMessagesDirectoryEntry[] theDirectory;
   private static final int DIRECTORY_OFFSET = 1;

   private static Logger log4j = Logger.getLogger(CrossReference.class);

   // Entered buildClassEnum()
   private enum CrossReferenceEnum {
      // This is a directory item. It's offset and length cannot be determined
      // until runtime.
      NAME(0, -1, 0, true),

      // This is a directory item. It's offset and length cannot be determined
      // until runtime.
      REFERENCED_OBJECT(1, -1, 0, true),

      // sizeInBits: 8
      MULTI(2, 2, 0, false);

      private final int id;
      private final int offsetInBits;
      private int length;
      private boolean isDirectoryEntry;

      CrossReferenceEnum(int id, int offsetInBits, int length, boolean isDirectoryEntry) {
         this.id = id;
         this.offsetInBits = offsetInBits;
         this.length = length;
         this.isDirectoryEntry = isDirectoryEntry;
      }

      public int getId() {
         return this.id;
      }

      public int getOffsetInBits() {
         return this.offsetInBits;
      }

      public int getLength() {
         return this.length;
      }

      public void setLength(int length) {
         this.length = length;
      }
   } // Leaving buildClassEnum()

   /*
    * The instance variables.
    * See buildInstanceVariables() in JBitByBitGenerator.java.
    */
   private String name;
   private String referencedObject;
   private boolean multi;

   private String theErrorMsg;

   /**
    * The CrossReference no-arg constructor.
    */
   public CrossReference() {}

   /**
    * The CrossReference constructor.
    * See buildParameterAnnotations() in JBitByBitGenerator.java.
    *
    * @param name The name message attribute.
    * @param referencedObject The referencedObject message attribute.
    * @param multi The multi message attribute.
    */
   public CrossReference(String name,
                         String referencedObject,
                         Boolean multi) {
      // Entered buildParameterAssignments()
      this.setName(name);
      this.setReferencedObject(referencedObject);
      this.setMulti(multi);
      // Leaving buildParameterAssignments()
   }

   /**
    * The CrossReference copy constructor.
    */
   public CrossReference(CrossReference crossReference) {
      // Entered buildCopyConstructorParameterAssignments()
      this.setName(crossReference.getName());
      this.setReferencedObject(crossReference.getReferencedObject());
      this.setMulti(crossReference.getMulti());
      // Leaving buildCopyConstructorParameterAssignments()
   }

   // Entered buildSetErrorMsgMethod()
   private void setErrorMsg(String errorMsg) {
      theErrorMsg = errorMsg;
   }
   // Leaving buildSetErrorMsgMethod()

   // Entered buildGetErrorMsgMethod()
   /**
    * This method returns the error message or null if no error
    * occurred.
    */
   public String getErrorMsg() {
      return theErrorMsg;
   }
   // Leaving buildGetErrorMsgMethod()

   // Entered buildPackMethod()
   /**
    * This method packs a CrossReference object into a byte array. If a required attribute is not
    * provided an exception will be thrown.
    *
    * @return A byte array which represents a CrossReference message object.
    * @throws MissingAttributeException
    */
   public byte[] pack() throws MissingAttributeException {
      final String METHOD = "pack()";
      log4j.debug("Entered " + METHOD);

      int byteArraySize = getCrossReferenceSize();
      log4j.debug("byteArraySize: " + byteArraySize);
      byte[] bb = new byte[byteArraySize];

      /*
       * Convert object to byte array.
       */
      int offset = 0;

      if (this.getMulti() != null) {
         bb = GreenballMessagesUtility.insertBitfield(bb, offset * 8, 1, this.getMulti() == true ? 1 : 0);
         offset += 1;
      } else {
         /*
          * Missing required attribute, generate an error message and throw
          * an exception.
          */
         setErrorMsg("Error packing object. The attribute \"multi\" is required, but no value was provided.");
         log4j.error(getErrorMsg());
         throw new MissingAttributeException(getErrorMsg());
      }


      CrossReferenceEnum.NAME.setLength(this.getName().length());
      CrossReferenceEnum.REFERENCED_OBJECT.setLength(this.getReferencedObject().length());

      int directorySize = getCrossReferenceDirectorySize();
      if (directorySize != 0) {
         // Create the directory
         theDirectory = new GreenballMessagesDirectoryEntry[directorySize];
         for (int i = 0; i < directorySize; i++) {
            theDirectory[i] = new GreenballMessagesDirectoryEntry();
         }

         // Fill the directory
         int index = 0;
         offset = DIRECTORY_OFFSET
               + GreenballMessagesDirectoryEntry.DIRECTORY_ENTRY_SIZE_IN_BYTES
               * theDirectory.length;

         for (CrossReferenceEnum crossReferenceEnum : CrossReferenceEnum.values()) {
            if (crossReferenceEnum.isDirectoryEntry == true) {
               int id = crossReferenceEnum.getId();
               int length = crossReferenceEnum.getLength();

               theDirectory[index].setId(id);
               theDirectory[index].setLength(length);
               theDirectory[index].setOffset(offset);

               // Insert the data
               if (id == CrossReferenceEnum.NAME.getId()) {
                  log4j.debug(String.format("inserting %s at offset %d", "name", offset));
                  bb = GreenballMessagesUtility.insertString(bb, offset, getName());
                  offset += length * 1;
               }
               else if (id == CrossReferenceEnum.REFERENCED_OBJECT.getId()) {
                  log4j.debug(String.format("inserting %s at offset %d", "referencedObject", offset));
                  bb = GreenballMessagesUtility.insertString(bb, offset, getReferencedObject());
                  offset += length * 1;
               }
               index++;
            }
         }

         // Insert the directory
         offset = DIRECTORY_OFFSET;
         for (GreenballMessagesDirectoryEntry directoryEntry : theDirectory) {
            bb = GreenballMessagesUtility.insertInteger(bb, offset, directoryEntry.getId());
            bb = GreenballMessagesUtility.insertInteger(bb, offset + 4, directoryEntry.getLength());
            bb = GreenballMessagesUtility.insertInteger(bb, offset + 8, directoryEntry.getOffset());

            offset += GreenballMessagesDirectoryEntry.DIRECTORY_ENTRY_SIZE_IN_BYTES;
            index++;
         }

      }
      log4j.debug("Leaving " + METHOD);
      return bb;
   }
   // Leaving buildPackMethod()

   // Entered buildUnpackMethod()
   /**
    * This method unpacks a byte array containing a CrossReference object.
    *
    * @return The byte array as a CrossReference message object.
    */
   public CrossReference unpack(byte[] ba) {
      final String METHOD = "unpack()";
      log4j.debug("Entered " + METHOD);

      CrossReference crossReference = new CrossReference();

      crossReference.setMulti(GreenballMessagesUtility.getBoolean(ba, CrossReferenceEnum.MULTI.getOffsetInBits()));

      // Get the directory
      int directorySize = getCrossReferenceDirectorySize();
      theDirectory = new GreenballMessagesDirectoryEntry[directorySize];
      int dirOffset = DIRECTORY_OFFSET;

      for (int entry = 0 ; entry < directorySize; entry++) {
         GreenballMessagesDirectoryEntry directoryEntry = new GreenballMessagesDirectoryEntry();

         int id = GreenballMessagesUtility.getInteger(ba, dirOffset * 8);
         directoryEntry.setId(id);
         int length = GreenballMessagesUtility.getInteger(ba, (dirOffset + 4) * 8);
         directoryEntry.setLength(length);
         int offset = GreenballMessagesUtility.getInteger(ba, (dirOffset + 8) * 8);
         directoryEntry.setOffset(offset);

         theDirectory[entry] = directoryEntry;

         dirOffset += GreenballMessagesDirectoryEntry.DIRECTORY_ENTRY_SIZE_IN_BYTES;
      }

      for (GreenballMessagesDirectoryEntry directoryEntry : theDirectory) {
         int id = directoryEntry.getId();
         int offset = directoryEntry.getOffset();
         int length = directoryEntry.getLength();

         if (id == CrossReferenceEnum.NAME.getId()) {
            crossReference.setName(GreenballMessagesUtility.getString(ba, offset, length));
         }
         else if (id == CrossReferenceEnum.REFERENCED_OBJECT.getId()) {
            crossReference.setReferencedObject(GreenballMessagesUtility.getString(ba, offset, length));
         }
      }

      log4j.debug("Leaving " + METHOD);
      return crossReference;
   }
   // Leaving buildUnpackMethod()

   /**
    * This method sets the name attribute. Setting of this value is required.
    * @param name The name attribute.
    */
   public void setName(String name) {
      this.name = name;
   }

   /**
    * This method sets the referencedObject attribute. Setting of this value is required.
    * @param referencedObject The referencedObject attribute.
    */
   public void setReferencedObject(String referencedObject) {
      this.referencedObject = referencedObject;
   }

   /**
    * This method sets the multi attribute. Setting of this value is required.
    * @param multi The multi attribute.
    */
   public void setMulti(Boolean multi) {
      this.multi = multi;
   }

   /**
    * This method gets the name attribute. The name attribute is a required attribute.
    */
   public String getName() {
      return this.name;
   }

   /**
    * This method gets the referencedObject attribute. The referencedObject attribute is a required attribute.
    */
   public String getReferencedObject() {
      return this.referencedObject;
   }

   /**
    * This method gets the multi attribute. The multi attribute is a required attribute.
    */
   public Boolean getMulti() {
      return this.multi;
   }

   /**
    * This method gets the size of the directory.
    */
   private int getCrossReferenceDirectorySize() {
      int directorySize = 0;

      // name
      directorySize++;
      // referencedObject
      directorySize++;

      return directorySize;
   }


   /**
    * This method gets the size of the CrossReference object.
    * @return The size in bytes.
    */
   public int getCrossReferenceSize() {
      int size = 0;

      int directorySize = getCrossReferenceDirectorySize();
      size += directorySize * GreenballMessagesDirectoryEntry.DIRECTORY_ENTRY_SIZE_IN_BYTES;
      // Add size of name
      size += (getName() != null ? getName().length() : 0);
      // Add size of referencedObject
      size += (getReferencedObject() != null ? getReferencedObject().length() : 0);
      // Add size of multi
      size += 1;

      return size;
   }

   @Override
   public String toString() {
      StringBuilder result = new StringBuilder();
      String NEW_LINE = System.getProperty("line.separator");

      result.append(this.getClass().getSimpleName() + " Object {" + NEW_LINE);
      result.append(" name: " + name + NEW_LINE);
      result.append(" referencedObject: " + referencedObject + NEW_LINE);
      result.append(" multi: " + multi + NEW_LINE);

      result.append("} ");

      return result.toString();
   }

   /**
    * The hashCode method returns this object's hashCode.
    */
   @Override
   public int hashCode() {
      final int prime = 31;
      int result = 1;
      result = prime * result
         + ((String) name).hashCode();
      result = prime * result
         + ((String) referencedObject).hashCode();
      result = prime * result
         + ((Boolean) multi).hashCode();
      return result;
   }

   /**
    * The equals method checks for equality. 
    */
   public boolean equals(Object obj) {
      if (obj == this) return true;
      if (obj == null) return false;
      if (getClass() != obj.getClass()) return false;

      CrossReference crossReference = (CrossReference) obj;
      if (!crossReference.getName().equals(this.getName())) {
         return false;
      }

      if (!crossReference.getReferencedObject().equals(this.getReferencedObject())) {
         return false;
      }

      if (crossReference.getMulti() != this.getMulti()) {
         return false;
      }

      return true;
   }
}
