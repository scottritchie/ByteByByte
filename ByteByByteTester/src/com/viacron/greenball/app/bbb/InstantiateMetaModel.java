/*
 * WARNING: This file was generated by BitByBitGenerator.xtend (see
 * generateInstantiationFile()). 
 * Do not edit.
 */
package com.viacron.greenball.app.bbb;
import java.util.ArrayList;
import java.util.List;

import org.apache.log4j.Logger;

import com.viacron.utils.StringMessageBuffer.parser.AbstractAttribute;
import com.viacron.utils.StringMessageBuffer.parser.Element;
import com.viacron.utils.StringMessageBuffer.parser.KeyValuePair;

public class InstantiateMetaModel {
   private static Logger log4j = Logger.getLogger(InstantiateMetaModel.class);

   /*
    * The isUpdated flags. If an attribute is required by the grammar then a value
    * must be provided, and the isUpdated flag will be set when the value is
    * obtained during unpacking. After the object is constructed during unpacking, 
    * validation will be performed to make sure a value was provided for all attributes
    * that are not optional. 
    * See buildFlags() in JBitByBitGenerator.java.
    */
   private boolean isIdUpdated = false;
   private boolean isPckgUpdated = false;

   private Element element;
   private MetaModel metaModel;

   /**
    * The InstantiateMetaModel class instantiates the MetaModel object.
    *
    * @param element The root element returned by the parser after parsing the string
    *                representing the MetaModel.
    */
   public InstantiateMetaModel(Element element) {
      this.element = element;   
      this.metaModel = new MetaModel();
   }

   /**
    * This method instantiates the MetaModel.
    *
    * @return The MetaModel.
    */
   public MetaModel instantiate() {
      final String METHOD = "instantiate()";
      log4j.debug("Entered " + METHOD);

      log4j.debug("MetaModel: " + element.getName());
      log4j.debug("# of attributes: " + element.getAbstractAttributes().size());

      instantiateMetaModelAttributes(element);

      for (AbstractAttribute abstractAttribute : element
            .getAbstractAttributes()) {
         for (Element e : abstractAttribute.getElements()) {
            log4j.debug("element name: " + e.getName());
      instantiateMetaModelSubTypeRefs(e);
      }
      }

      log4j.debug("Leaving " + METHOD);
      return metaModel;
      }

      /*
       * This method instantiates the MetaModel attributes.
       * 
       * @param Element element
       */
      private void instantiateMetaModelAttributes(Element element) {
      final String METHOD = "instantiateMetaModelAttributes()";
      log4j.debug("Entered " + METHOD);

      log4j.debug("element name: " + element.getName());
      for (AbstractAttribute attr : element.getAbstractAttributes()) {
         for (KeyValuePair kvp : attr.getKeyValuePairs()) {
            log4j.debug("key: " + kvp.getKey());
            log4j.debug("value: " + kvp.getValue());
            // Entered instantiateMsgAttribute()
            if (kvp.getKey().equals("id")) {
               log4j.debug("setting value of \"id\" to: " + kvp.getValue());
               this.isIdUpdated = true;
               metaModel.setId(kvp.getValue());
            }
            // Leaving instantiateMsgAttribute()
         }
      }

      log4j.debug("Leaving " + METHOD);
   }

   /*
    * This method instantiates the MetaModel SubType references.
    * 
    * @param Element element
    */
   private void instantiateMetaModelSubTypeRefs(Element element) {
      final String METHOD = "instantiateMetaModelSubTypeRefs()";
      log4j.debug("Entered " + METHOD);

      // Entered instantiateMessageSubTypeRefList()
      if (element.getName().startsWith("pckg[")) {
         log4j.debug("found element: pckg");
         this.isPckgUpdated = true;
         instantiatePckg(element);
         return;
      }
      // Leaving instantiateMessageSubTypeRefList()


      log4j.debug("Leaving " + METHOD);
   }

/*
 * This method instantiates the pckg attribute which is a list of PackageSubType
 * @param Element element
 */
private void instantiatePckg(Element element) {
      // Entered instantiateSubTypeRefList()
   final String METHOD = "instantiatePckg()";
   log4j.debug("Entered " + METHOD);

   if (metaModel.getPckg() == null) {
   metaModel.setPckg(new ArrayList<PackageSubType>());
   }

   InstantiatePackageSubType instantiatePackageSubType = new InstantiatePackageSubType(element);
   PackageSubType packageSubType = instantiatePackageSubType.instantiate();
   metaModel.getPckg().add(packageSubType);
   log4j.debug("Leaving " + METHOD);
   // Leaving instantiateSubTypeRefList()
}

   /**
    * The validate method checks that a value was provided for all required 
    * attributes.
    *
    * @return The error message or null if no error occurred.
    */
   protected String validate() {
      final String METHOD = "validate()";
      log4j.debug("Entered " + METHOD);

      String result = null;

      // Entered buildValidationMethod()
      if (!this.isIdUpdated) {
         String msg = "Id is required, but no value was provided.";
         if (result == null) {
            result = msg;
         }
      }

      if (!this.isPckgUpdated) {
         String msg = "Pckg is required, but no value was provided.";
         if (result == null) {
            result = msg;
         }
         else {
            result = result.concat("\n" + msg);
         }
      }


      if (result != null) {
         log4j.debug("result: " + result);
      }

      log4j.debug("Leaving " + METHOD);
      return result;
   }
}
